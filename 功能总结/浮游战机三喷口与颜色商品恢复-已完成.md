# 浮游战机三喷口与颜色商品恢复（已完成）

## 最终结果

11.3/GitHub 离线版本已经成功加入 2.5 版本三尾浮游战机的独立外观路径。该路径只改变外观，沿用商店购买和 `subCarLabel` 即时换肤逻辑，不生成背包物品。

```text
蓝尾浮游战机TVC    -> subCar_tvc_blue    -> sub25/subCar_blue
红尾浮游战机TVC    -> subCar_tvc_red     -> sub25/subCar_red
金尾浮游战机TVC    -> subCar_tvc_yellow  -> sub25/subCar_yellow
```

现有 11.3 的 `subCar_blue`、`subCar_red`、`subCar_yellow` 均保留不变。

## 资源方案

新增独立资源组，不覆盖 `sub1130.swf`：

```text
metalwartale3-reborn.git\swf\sub25.swf
```

文件来源：

```text
原版\2.5（原版参考）\2.5版本素材库\raw\swf\sub37.swf
```

资源保留 2.5 的 MovieClip、Shape、时间轴、六帧尾焰动画和 `armsPoint` 结构。SHA-256：

```text
9DBFEFC830045E8453763504AC27F37F186679A52BD17A938884FFD613FF392B
```

## 代码路径

`Game.as` 加载 `swf/sub25.swf`，资源组名为 `sub25`：

```text
decompiled\gamefile\scripts\Game.as
```

`BodyGroup.as` 在玩家和敌方路径注册三种 2.5 符号，并映射为 `subCar_tvc_blue`、`subCar_tvc_red`、`subCar_tvc_yellow`：

```text
decompiled\gamefile\scripts\bodyGroup\BodyGroup.as
```

`HighPlayerBox.as` 支持三个 TVC 标签，并按颜色从 `sub25` 资源组读取主界面外观：

```text
decompiled\gamefile\scripts\UI\top\HighPlayerBox.as
```

商店购买路径仍使用 `GameData.subCarLabel` 和 `Game.eventGroup.fleshSub()`，相关代码位于 `OneShopUI.as` 与 `GameData.as`。

## 商店配置

商品定义：

```text
decompiled\embedded-xml-assets\19_EmbedXml_xmlClass12_EmbedXml_xmlClass12.bin
```

三件 TVC 均使用 `<price>1</price>`，图标和展示资源分别为 `sub25/subCar_blue`、`sub25/subCar_red`、`sub25/subCar_yellow`。

商店分类：

```text
decompiled\embedded-xml-assets\21_EmbedXml_xmlClass13.bin
```

`GCoin.props` 已加入三个 TVC 标签，原有三种鸡翅浮游战机也仍在列表中。

## 构建与验证

构建使用 `scripts\build_swf.bat`，最终 `build\game.swf` SHA-256：

```text
9F6078D21EDB0B76BEC5BC86998549ABF00CF89F3F26BD63E89C62AFE81682F5
```

最终 SWF 已通过 FFDec 反解确认包含三条 TVC 商品记录、中文名称和 `sub25` 图标路径。`swf\sub25.swf` 已同步到 `build\swf\sub25.swf`，两份文件哈希一致。蓝尾 TVC 已在实际游戏中确认显示，红尾和金尾沿用同一套资源、注册和切换路径完成导入。

## 维护注意事项

- 不要删除或覆盖 `swf\sub25.swf`。
- 不要把 2.5 尾焰 PNG 单独挂到战斗场景，必须调用 `sub25/subCar_*` MovieClip。
- 修改 ActionScript 后使用 `scripts\build_swf.bat`，确保 BinaryData patch 生效。
- 11.3/GitHub 与 2.5 是不同运行实例，测试时不要混用 Flash 进程或端口。

