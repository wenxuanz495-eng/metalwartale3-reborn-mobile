# 调皮家族（弹性榴弹发射器）三项 2.5 回迁执行总结

> 执行日期：2026-09-09（副武器第三家族）
> 用户决策范围：①射速回到 2.5 ②受击特效回到 2.5 ③待机/开火状态＋枪口火焰（含开火音效）全部回到 2.5
> 前置审计：《调皮家族2.5-3.4-11.3三版本对比总结-20260909.md》（同目录）
> 工作区：`D:\superalloy\tmp-soya-family-test\elastic-family-audit\work-restore\`
> 禁区遵守：经济/成长字段零改动；`boom_hit_effect`/`positron_hit_effect` 共用资源内容零改动；未移除任何既有定义（旧焰效件留作孤儿）

## 一、目标

把调皮家族（`arms id="elastic"`，四级 调皮/淘气/捣蛋/顽皮）以下三项恢复为 2.5 原版表现：

1. **射速**：`attackGap` 0.9 → 0.4（四级统一，2.5=3.4 均为 0.4）；
2. **受击特效**：紫色小光爆 `bullet/purple_boom` → 2.5 七帧橙色像素爆炸＋专属爆炸音；
3. **开火状态/枪口火焰/开火音效**：GH 缺失的 f2~f7 六帧开火换体（后坐姿态）动画恢复，本体时间轴整段对齐 2.5（含 f8~f14 枪口焰序列原帧位），开火音换回 2.5 原音数据。

## 二、实际修改

### 2.1 SWF：`swf\sub1130.swf`（= `build\swf\` = `runtime\swf\` 三处同步）

方式：响尾蛇先例的"时间轴整段替换＋闭包后序导入＋挂点矩阵强制当前值"，脚本 `elastic_restore_merge.py`（同目录）。

- 四级本体时间轴替换（GH 精灵 ← 2.5 模板）：`elastic_lv1` 1363←1374、`elastic_lv2` 1357←1347、`elastic_lv3` 1347←1320、`elastic_lv4` 1341←1293；每级 15 帧，f2~f7 开火换体六帧＋f7 开火音＋f8~f14 枪口焰/消散七帧＋f15 清理，与 2.5 逐帧事件签名一致（脚本内断言通过）。
- 新增定义 **117 项，新 ID 区间 1717–1833**：2.5 时间轴闭包（待机体 shape+bitmap、四级 A 序列换体 shape+JPEG3 bitmap、四级 B/C 枪口焰 shape+bitmap 共 116 项）＋开火音效克隆（2.5 sound 1279，数据哈希 `46AC5021FD7B06A6`，sub1130 无等音 → 新 ID **1833**）。
- 挂点：2.5 的 110 映射到当前版 104，`basePoint`/`shootPoint` 矩阵强制为当前值（两版本矩阵本就逐字节相同，射点零漂移）。
- 导出表（SymbolClass）**零变化**；无重复 spriteId；新 ID 无冲突；回读 XML 零悬空引用。

### 2.2 配置：`decompiled\embedded-xml-assets\6_EmbedXml_xmlClass7_EmbedXml_xmlClass7.bin`

脚本 `elastic_config_patch.py`（同目录），只在 elastic `<arms>` 块内替换（块外零变化断言通过）：

- `attackGap` `0.9`→`0.4` ×4（等长）；
- `hitImgLabel` `bullet/purple_boom`→`sub/positron_hit_effect` ×4（+5 字节/处，共 +20 字节）。
- **受击零资源成本路线**：GH sub1130 现有导出 `positron_hit_effect`（精灵 1702，响尾蛇家族回迁产物）经逐帧渲染比对与 2.5 `boom_hit_effect`（1678）**七帧全部 0.0000 差异**，且其内嵌音效 1716 即 2.5 原爆炸音数据（`C07C4D69595E3624`）；GH 现有同名 `boom_hit_effect`（1454）与 2.5 为同名不同物（渲染差 3.5%~29.6%、音效不同），故不可直接改绑它，改绑 1702 等效于完整回到 2.5。

### 2.3 Manifest

`config\build\current-resource-manifest.sha256`：`swf\sub1130.swf` 哈希 `78979D07…` → `55148C51E094104BCA46A8394DAF7C2D885517F132FFAEBA256EDFFB66A9E65F`。

### 2.4 备份（同目录 `备份-被替换的旧资源-20260909\`）

- `sub1130-回迁前原版.swf`（3559701 字节，修改前 swf\ 原件）
- `6_EmbedXml_xmlClass7-回迁前原版.bin`（291731 字节，sha 前 16 `BF1FD62F9CEA2144`）

## 三、构建与验证

| 验证项 | 结果 |
|---|---|
| xml2swf 重建 | 退出码 0，`sub1130-restored.swf` 3672822 字节 |
| swf2xml 回读 | elastic 四级 15 帧/16 放置/3 移除/1 开火音（1833）＝2.5 结构；导出表零变化；零悬空引用（move 型 characterId=0 已排除） |
| 渲染像素比对 | 回迁后 sub1130 的 lv1~lv4 逐帧 GIF vs 2.5 对应级：**4 级 × 15 帧全部 max_diff=0.0000** |
| 三处同步 | `swf\`＝`build\swf\`＝`runtime\swf\`＝55148C51… 一致 |
| 构建 | `构建.bat` 退出码 0，175 运行时资源校验通过，`build\game.swf` 生成（FA165B216285D306…） |
| BinaryData 回读 | `build\game.swf` 内 elastic 块：`attackGap 0.4` ×4、`0.9` 残留 0、`sub/positron_hit_effect` ×4、`purple_boom` 残留 0 |
| 双自检 | `launch_game.bat --check sa` 退出码 0；`--check sa_debug` 退出码 0 |

## 四、待实机确认项

1. 射速实战计时：attackGap 0.4 的实际发/秒（面板≠实战）。
2. 受击橙爆＋爆炸音、开火换体动画、枪口焰时序、开火音的实机观感。
3. 反弹榴弹（floorBounce/gravity 未动）在新射速下的手感。
4. 旧焰效孤儿件（GH 原 B/C 序列 shape/bitmap、旧待机体等，均零引用化但保留在文件内）后续确认无用途后可按规范备份移除。

## 五、遗留说明

- 本家族受击与响尾蛇共用 `sub/positron_hit_effect`（公共受击特效先例，同 `motion_hit_effect` 机制）；如未来需区分两家族爆炸表现，再行拆分。
- GH sub1130 内旧的同名 `boom_hit_effect`（1454）未动，仍供其他引用使用。
