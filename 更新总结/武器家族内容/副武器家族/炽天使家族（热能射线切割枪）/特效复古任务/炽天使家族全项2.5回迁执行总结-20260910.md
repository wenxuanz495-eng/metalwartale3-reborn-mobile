# 炽天使家族（热能射线切割枪）2.5 全项回迁执行总结
> ✅ **2026-09-10 实机确认定稿**：全项 2.5 回迁全部项目经玩家实机验证通过（含连续射击音效触发），本总结转为定稿归档。

> 执行日期：2026-09-10
> 性质：✅ **已构建入库待实测**（用户决策：①待机/炮身回 2.5 ②受击回 2.5 ③射速回 2.5，即全项 2.5 复原）
> 前置：`炽天使家族2.5-3.4-11.3三版本对比总结-20260909.md`（只读对比定案）
> 工作区：`D:\superalloy\tmp-soya-family-test\hotline-3ver-audit\`（合并脚本 `hotline_restore_merge.py`）

## 一、目标

按用户决策将副武器 `arms id="hotline"`（炽天使/伊夫利特/不死鸟/洛基/凤凰五形态）的视听元素与射速回迁至 2.5 版本：

1. 本体时间轴（含全部 blend=8 待机/炮身光斑层、脉冲扫掠系列）回 2.5；
2. 受击特效 `hot_hit_effect` 回 2.5 六帧六态；
3. `attackGap` 2.4→1.5 ×5。

## 二、实际修改

### 2.1 sub1130.swf（8D885B69 → 6C6B824E）

合并脚本一次性完成（全程单棵 ElementTree，无重读原始 XML）：

- **同美术复用映射 62 对**（逐对哈希断言）：本体形状 954→1162 等 ×5、光束 695→1067、lv1/lv2 烟雾循环 956/964/966/974→1164/1166/1168/1170、lv3~5 脉冲系列 790~859→1112~1148 与 701~764→1070~1103、受击态1 1645→1433；挂点 110→104（标记图形三版本就不同，按名映射、矩阵强制当前值）。
- **闭包导入 264 项**（新 Character ID **2030～2293**）：lv1/lv2 全部 34 个枪口/炮身光斑（精灵+形状+位图三件套）、lv3~5 炮身光斑系列、受击第 2/4/6 态三张 JPEG3（1646/1650/1654→新 ID）+包裹形状（1647/1651/1655→新 ID）。
- **时间轴整段替换**：lv1~lv5 五个本体精灵 subTags 换 2.5 模板（帧数 30 不变，挂点矩阵强制回迁前当前值）；hot_hit_effect subTags 换 2.5 模板（6 帧 6 态；态交换为 move 型放置，态3/5 走导入路径渲染等价）。
- **SymbolClass 全表 253 项零变化**（六导出名绑定不变，无新增导出名，无 ABC 桩问题）。

### 2.2 配置 6_EmbedXml_xmlClass7（78B21BE2 → E5F1DB0E，291717 字节不变）

hotline 块（index=5）内 `<attackGap>2.4</attackGap>` → `<attackGap>1.5</attackGap>` ×5，等长替换；块外区域零字节变化（脚本断言）。`hitImgLabel` 配置无需改动（受击回迁是资源层替换）。

### 2.3 三处同步与登记

`swf\` = `build\swf\` = `runtime\swf\` 三处 sub1130 均 = 6C6B824E…；`config/build/current-resource-manifest.sha256` sub1130 行已更新；`docs/CURRENT_RESOURCE_MANIFEST.md` 追加本次登记。

## 三、构建与验证

| 验证项 | 结果 |
|---|---|
| 回读断言：SymbolClass 全表 253 项一致 | PASS |
| 回读断言：lv1~lv5+受击 帧数/放置/移除/有内容帧 == 2.5（114/114/99/43/43/6 放置） | PASS |
| 回读断言：五级挂点命名齐、矩阵与回迁前逐字节一致 | PASS |
| 回读断言：受击独立态 = 6 | PASS |
| 回归断言：其他家族 16 符号（cutter/elastic/laser/lightningBall/elecGun/missile/gold/受击集）零变化 | PASS |
| **渲染验证：回迁后五级本体全 30 帧 + 受击 6 帧 vs 2.5 逐像素 0 差异** | **PASS** |
| 配置回读：hotline 块 attackGap 1.5×5、块外零变化 | PASS |
| 构建 `构建.bat` | 退出码 0 |
| 构建产物 game.swf（046A0AE9…）解压回读：hotline 块 1.5×5 + 受击绑定 ×5 | PASS |
| `launch_game.bat --check sa` / `--check sa_debug` 双自检 | 双 PASS |
| 旧资源备份 | `备份-被替换的旧资源-20260910\`（sub1130=8D885B69、config=78B21BE2） |

证据图：`炽天使回迁前后待机对比图-20260910.png`（lv1 待机枪口区三格对比）。

## 四、待实机确认项

- [ ] 待机态：lv1/lv2 枪口光点、lv3~5 炮身光斑是否如 2.5 实机观感恢复；
- [ ] 开火态：沿激光束的 ADD 闪光/火花层（lv4/lv5 尤其明显）实机动态；
- [ ] 受击：六态闪烁演化是否流畅（原三态跳变是否消失）；
- [ ] 射速：attackGap 1.5 实机发射节奏（面板值≠实战值，建议计时验证）；
- [ ] 穿透激光线（penetrationB=1 + MaskArmsImage 光束展开）在光斑层回迁后无遮挡/错位异常。

## 五、禁区与遗留

- 经济/成长字段（price/mustItems/commonLevel）按用户禁令未动；
- GH 旧版被替换前的光斑缺失状态无孤儿资源产生（旧时间轴被整段替换，其引用的既有资产仍被新时间轴复用）；
- 3.4 与 2.5 本体本就一致，本次回迁同时等于 3.4 状态。