# 高周波切割炮家族四项 2.5 回迁执行总结

> 执行日期：2026-09-09（副武器第五家族）
> 用户决策范围：①子弹回 2.5 五级独立 ②受击（六态特效＋受击音频）回 2.5 ③待机/开火（含充能序列）回 2.5 ④枪口火焰回 2.5；LV6 深渊MK2 用 LV5 的特效/子弹/开火音/音效（本体保留 MK2 自身配色）。射速/分散/弹速不动（attackGap GH 已同 2.5 的 0.9；reduceRa/attackType 维持现状）。
> 前置审计：《高周波切割炮家族2.5-3.4-11.3三版本对比总结-20260909.md》（同目录）
> 工作区：`D:\superalloy\tmp-soya-family-test\laser-family-audit\`（脚本 `cutter_restore_merge.py`/`cutter_config_patch.py` 已随归档入库）

## 一、实际修改

### 1.1 SWF：`swf\sub1130.swf`（= `build\swf\` = `runtime\swf\` 三处同步，哈希 `55148C51…`→`8F1C24B1FDFACEA3`）

脚本 `cutter_restore_merge.py`（响尾蛇/调皮"整段替换＋闭包导入"机制），新 ID 区间 **1834–1987 共 154 项定义**：

1. **待机/开火（含充能序列）**：lv1~lv5 本体时间轴整段替换为 2.5 模板（1260←1187、1241←1158、1224←1129、1207←1100、1204←1097），完整充能序列恢复（lv1 6 帧、lv2/3 13 帧、lv4/5 12 帧）＋开火合体帧＋焰/切序列原帧位；挂点 110→104 矩阵强制当前值。
2. **枪口火焰**：随本体时间轴整段回迁（2.5 的焰/切序列与 GH 本就同源，现帧位与充能节奏完全对齐 2.5）。
3. **开火音效**：2.5 开火音 1049（`F28AC2F6`，GH 无等音）克隆为新 ID **1986**，五级时间轴 StartSound 改指。
4. **子弹 lv1~lv5**：原位内容替换为 2.5 五级独立弹（52×30→112×64 逐级变大；导出名/ID 不变，`cutter_lv*_bullet` 仅本家族自用已核查）。MK2 子弹 `cutter_lv6_bullet`(1174) 按决策原位替换为 2.5 lv5 弹（`A0C2845D`）。
5. **受击（六态＋音频）**：`cutter_hit_effect` 被大量共用（xmlClass8 415 处/xmlClass7 34 处/xmlClass9 敌人 5 处/xmlClass3 主武器 2 处），**禁止原位替换**——导入 2.5 六态受击闭包（6 shape＋6 位图）建新精灵 **1985**，死名改绑 `cut_effect`(69)（配置＋脚本＋存档全域零引用已核查）；受击音 2.5 1608（`AB4493A4`，GH 无等音）克隆为新 ID **1987**，随新精灵时间轴播放。
6. **深渊MK2（1201）**：时间轴整段替换为 2.5 lv5 模板（充能/焰序列与 lv5 完全一致），**待机体经专用映射 1096→1185 保留 MK2 自身美术**；子弹/开火音/受击均如上走 lv5 同款。

### 1.2 配置：`decompiled\embedded-xml-assets\6_EmbedXml_xmlClass7_EmbedXml_xmlClass7.bin`

脚本 `cutter_config_patch.py`：cutter 块内 `<hitImgLabel>sub/cutter_hit_effect</hitImgLabel>`→`<hitImgLabel>sub/cut_effect</hitImgLabel>` ×6（lv1~lv6；-9 字节/处，bin 291751→291709 字节；块外零变化断言通过）。哈希：`CFE4B278ECEF35A9`（战殇补丁后版）→ `8D40ABEE057292E3`。

### 1.3 Manifest 与备份

- `config\build\current-resource-manifest.sha256`：`swf\sub1130.swf` → `8F1C24B1FDFACEA30F0ADD58859156C7CE5580A7CAA98618F785696278B1819F`
- 备份（同目录 `备份-被替换的旧资源-20260909\`）：`sub1130-回迁前原版.swf`（elastic 回迁后版）、`6_EmbedXml_xmlClass7-回迁前原版.bin`

## 二、构建与验证

| 验证项 | 结果 |
|---|---|
| xml2swf 重建 | 退出码 0；swf2xml 回读零悬空引用、spriteId 唯一、导出表仅 `cut_effect` 69→1985 一处变化 |
| 渲染像素比对 | 回迁后 lv1~lv5 vs 2.5 对应级：**5 级 × 15 帧全部 0.0000**；五级子弹 vs 2.5：**全 0.0000**；MK2 弹(=lv5 弹) **0.0000**；新受击精灵 vs 2.5 六态：**0.0000**；MK2 f1=0.0250（MK2 自身待机体，预期保留项），f2~f15 差异为 MK2 本体配色透过特效可见（目检确认特效与 lv5 完全一致） |
| 三处同步 | `swf\`＝`build\swf\`＝`runtime\swf\`＝8F1C24B1… 一致 |
| 构建 | `构建.bat` 退出码 0（中途因自检残留 server.exe 占用报 code 4，清理后台进程后重建成功），`build\game.swf` 生成（F1EECA61C7F42DDB…） |
| BinaryData 回读 | cutter 块 `sub/cut_effect` ×6、`cutter_hit_effect` 残留 0；顺带复核 elastic（0.4×4/positron×4）与 laser（1.5×4）回迁完好 |
| 双自检 | `--check sa` / `--check sa_debug` 均退出码 0 |

## 三、待实机确认项

1. 充能序列回 2.5 后四级蓄力节奏与 attackGap 0.9 的发射时机观感。
2. 受击六态细线切割＋受击音（2.5 原音）实机听感。
3. 五级独立弹（逐级变大）实机观感；MK2 使用深渊同款弹与特效的观感。
4. `reduceRa=0.5`（三版三方，按决策维持现状）的跟踪减速手感是否可接受。
5. 旧资源（GH 原共用受击 1509 内容未被触碰、旧四级共用弹、旧开火音 1186 定义等）保留在文件内未删，长期确认无用后可按规范备份清理。

## 四、遗留说明

- `cutter_hit_effect`（1509）内容未动，仍供其余数百处共用引用使用；本家族 6 级改指新精灵 `cut_effect`(1985)。
- `cutter_gold` 金切割体系为独立 arms，本次未涉及。
