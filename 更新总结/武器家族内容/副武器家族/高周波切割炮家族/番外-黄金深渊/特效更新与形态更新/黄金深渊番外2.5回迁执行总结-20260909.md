# 番外·黄金深渊 2.5 回迁执行总结

> 执行日期：2026-09-09（高周波切割炮家族番外）
> 用户决策范围：**一切视觉听觉元素回 2.5**（充能橙核辉光、焰/切第二层、开火音、子弹、受击六态＋受击音）；等级（commonLevel/mustLevel/Level_Growth_50 成长型）、战斗力与材料类字段（price 等）不动
> 前置审计：《黄金深渊番外2.5-3.4-11.3三版本对比总结-20260909.md》（同目录）
> 工作区：`D:\superalloy\tmp-soya-family-test\laser-family-audit\`（脚本 `gold_restore_merge.py`/`gold_config_patch.py` 已随归档入库）

## 一、实际修改

### 1.1 SWF：`swf\sub1130.swf`（= `build\swf\` = `runtime\swf\` 三处同步，哈希 `8F1C24B1…`→`C89B9C80EA76CBC9`）

脚本 `gold_restore_merge.py`。**关键复用**：gold 模板的充能/焰切链与 2.5 lv4/lv5 同源（同 char），本家切割炮回迁时已全部迁入 GH——用「2.5 lv4 链 ↔ 回迁后 GH lv4 链」按放置顺序对齐反推既有映射（**逐对位图哈希断言通过**），零重复导入：

1. **本体（1266）时间轴整段替换**为 2.5 gold 模板（1079）：充能橙核五态（f2~f6：`B4E10134/A357D406/A15CE94D/1E8981BB/20F2CB00`）＋焰/切七帧双位图序列（f7~f13：`9739D9D8+5AB9CF4D → … → A9DFA3C0+7E1CAF26`）全部恢复；挂点 110→104 矩阵强制当前值。
2. **待机体**：2.5 gold 待机（1046，金色 `822A9B84`＝GH 既有同图）整只导入新 ID（1988 区段）——注册点随 2.5 模板保真。
3. **开火音**：直接映射到本家切割炮回迁已克隆的 2.5 开火音 **1986**（`F28AC2F6`），不再重复克隆；时间轴 StartSound 由 f2 播放（与 2.5 gold 原时序一致）。
4. **子弹**：`cutter_gold_bullet`(1263) 原位内容替换为 2.5 gold 弹（1044：104×59，GH 的 77×48 重制版弃用）；`cutter_gold_bullet` 仅金切割自用（配置 1 处引用），原位替换安全。

新 ID 区间 **1988–1992 共 5 项定义**（gold 待机 shape+bitmap、gold 弹 shape+bitmap 等）。

### 1.2 配置：`decompiled\embedded-xml-assets\6_EmbedXml_xmlClass7_EmbedXml_xmlClass7.bin`

脚本 `gold_config_patch.py`：cutter_gold 块内 `<hitImgLabel>sub/cutter_hit_effect</hitImgLabel>`→`<hitImgLabel>sub/cut_effect</hitImgLabel>` ×1（-7 字节；块外零变化断言通过）。哈希：`8D40ABEE057292E3`（战殇＋本家补丁后版）→ `1826FB4459AA4771`。

- 受击六态＋受击音随 `cut_effect`(1985) 生效——本家回迁新建的 2.5 六态克隆（已验证与 2.5 逐像素一致、内嵌 2.5 原版受击音 1987），**零新增资源**。
- 保留不动：`specialType=Level_Growth_50` 成长型（GH 玩法机制）、`commonLevel=75`/`mustLevel=60`/`price=1000000`（等级与经济）、`attackGap=0.9`（已同 2.5）、`reduceRa=0.7`/`attackType=motion`（玩法参数，非视听元素）。

## 二、构建与验证

| 验证项 | 结果 |
|---|---|
| 链映射反推 | 12 对充能/焰切链 char 逐对位图哈希断言全部通过 |
| xml2swf 重建 | 退出码 0；swf2xml 回读零悬空引用、spriteId 唯一、导出表零变化 |
| 渲染像素比对 | 回迁后 gold 本体 vs 2.5 模板：**15 帧全部 0.0000**；gold 弹 vs 2.5 弹：**0.0000** |
| 三处同步 | `swf\`＝`build\swf\`＝`runtime\swf\`＝C89B9C80… 一致 |
| 构建 | `构建.bat` 退出码 0，`build\game.swf` 生成（8956554857291401…） |
| BinaryData 回读 | cutter_gold 块 `sub/cut_effect` ×1、`cutter_hit_effect` 残留 0；顺带复核本家 cut_effect×6、elastic 0.4×4、laser 1.5×4 回迁完好 |
| 双自检 | `--check sa` / `--check sa_debug` 均退出码 0 |

## 三、待实机确认项

1. 充能橙核＋焰切第二层恢复后的实机观感（金色本体＋橙色充能辉光）。
2. 子弹恢复 104×59 大小、开火音（2.5 原音）与受击六态＋受击音的实机听感观感。
3. `Level_Growth_50` 成长型在视听元素全 2.5 化后的成长表现（成长机制本身未动）。

## 四、遗留说明

- `cutter_hit_effect`（1509）内容始终未动，其余共用引用不受影响。
- GH 原 gold 弹（77×48）、旧开火音引用等保留在文件内未删，长期确认无用后可按规范备份清理。
