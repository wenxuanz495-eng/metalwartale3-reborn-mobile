# 金色发射特效导入执行总结（黄金飞刃炮两级）- 20260916

## 一、目标

用户指示：画师在 `相关素材\黄金恶魔爪拓展\` 交付了金色发射特效（`发射能量1~8.png` 八帧序列），先不做该目录中的新形态（黄金地狱之触/深渊之刃/灭世之手/诸神之殁），把金色发射特效放到**黄金恶魔牙（goldflyBlade_lv1=391）与黄金撒旦之力（goldflyBlade_lv2=399）**两级上试。

## 二、实际修改

**素材与现状的对位关系（导入前实测，本方案的事实基础）**：

- 现行开火大圈链＝8 个单帧精灵 **2650/2655/2660/2665/2670/2675/2680/2683**（20260912 第五批 2.5 套链产物），挂在 391 的 depth17 f3~f10 与 399 的 depth4 f3~f10，blend=5；
- 画师 8 帧 vs 现行链渲染（FFDec 全量精灵导出）：**1~6 号位画布尺寸与 alpha 通道逐位一致**（51x34/51x38/50x41/48x48/64x57/95x80，alphaMaxDiff=0，即画师对现行渲染逐帧重绘上色）；**7/8 号位为画师新构图**（93x59/95x56，现行 2680/2683 为 105x43/116x46 横向拖尾），构图同为金色拖尾；
- `sub1130.swf`（守望者导入后底稿 **4784C902**，manifest 断言 PASS）。

**结构手术**（`goldflyblade_gold_launchfx_import.py`，沿守望者 debris3 先例：FFDec swf2xml→单棵 ElementTree→xml2swf）：

1. **24 项新 Character ID 2732~2755**：每帧一位图＋一形状＋一单帧精灵；位图为 DefineBitsLossless2 预乘 ARGB（fmt5，字节序与预乘经守望者位图 29/2714 先例实证），形状沿 shape30 位图 1:1 矩形包裹先例（fillStyleType=67），精灵属性照单帧链精灵 2650 构造；定义插在 sprite391 之前（定义先于引用）；
2. **16 处放置改指**：391 d17 与 399 d4 的 f3~f10 放置 characterId 2650→2734、2655→2737、2660→2740、2665→2743、2670→2746、2675→2749、2680→2752、2683→2755；
   - **1~6 号位原矩阵零位移替换**（新旧画布/alpha 全等，实测质心差 ≤0.05px→Δ≤1 twip）；
   - **7/8 号位按 alpha 质心对位平移**（视觉中心 ≠ 包围盒中心，沿 hilt_center_tool 口径）：f9 Δ=(+118,−154) twips、f10 Δ=(+192,−94) twips（391/399 各自原矩阵同值叠加）；
   - blend=5 等放置属性原样保留；
3. **不动项**：小白圈蓄力链（2652~2677，391 d4 / 399 d6，f2~f7）、f2 圆饰环（2709 ×4，blend=8）、本体/挂点、StartSound 366；**共享大圈链的本体家族 flyBlade lv1~6（882/885/888/891/894/897）零改动**（引用断言 PASS）；
4. 回归断言：容器级逐项 diff＝2733→2757 项（+24 全为新定义），非预期差异仅 391/399 两项，其余 544 个既有精灵零变化，SymbolClass 原样；
5. `sub1130.swf`：4784C902 → **E7164ABF**（`E7164ABFBAF3720E602AA952C2B6E92AF5DAB2B013ACD3AD7638B0542EB49EB8`），三处同步（`swf\`＝`build\swf\`＝`runtime\swf\`）＋manifest 第 3 行更新。

## 三、构建与验证

- `构建.bat` 全量构建退出码 0（175 项运行时资源校验通过；game.swf 不变属预期——本批纯资源包改动）；
- `scripts\launch_game.bat --check sa` 与 `--check sa_debug` 双自检退出码均 0；
- FFDec 回读：新 SWF 全量精灵重导出，391/399 的 11 帧渲染与改前并排比对（`金色发射特效导入对比-lv1-391-前后11帧-20260916.png`、`金色发射特效导入对比-lv2-399-前后11帧-20260916.png`）——蓝白辉光链整体转金，帧位/节奏/剑柄小白圈与改前逐帧对应，7/8 号位金色拖尾落在枪头延长线原质心位。

## 四、待实机确认项

1. 两级开火：金色八帧链观感（细环→炽亮→白热峰值→暗淡消散）是否达到"黄金版"预期，与金色本体的配色协调度；
2. 峰值帧（f8 位置，95x80）幅度是否过大/过小，7/8 号位新构图拖尾的方向感是否自然；
3. 本体家族（恶魔牙 lv1~6）开火特效回归：仍为原蓝白链、无任何变化；
4. 20260914 三连修遗留待实机项继续有效：剑柄聚能观感、峰值光球幅度、镜像关卡、399 圈链对心决策（本批未动 399 原锚点语义，对心另行决策）。
