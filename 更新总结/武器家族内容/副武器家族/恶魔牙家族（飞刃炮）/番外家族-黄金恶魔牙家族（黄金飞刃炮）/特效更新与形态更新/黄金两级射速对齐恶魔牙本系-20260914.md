# 黄金两级射速对齐恶魔牙本系执行总结

> 完成日期：2026-09-14
> 任务：将黄金恶魔牙、黄金撒旦之力的射速调整为与恶魔牙系列本身一模一样
> 部署版本：配置 bin `8B6C5E51...`（原 `F7BEA1B0...`）、`build\game.swf` **C07CAC3E...**（原 `200E7752...`）

## 一、目标

用户指示：黄金恶魔牙（goldflyBlade lv1）与黄金撒旦之力（goldflyBlade lv2）射速对齐恶魔牙本系（flyBlade lv1~6）。此前口径为黄金维持 1.4/0.1（第四批记录"黄金射速维持 1.4/0.1 待决策"），本次为该决策的正式落定。

## 二、实际修改

配置对比（`decompiled\embedded-xml-assets\6_EmbedXml_xmlClass7_EmbedXml_xmlClass7.bin`，`<arms index="9">` 两个块逐字段核对）：

| 块 | attackGap | attackDelay | reduceRa | bulletNum |
|---|---|---|---|---|
| flyBlade lv1~lv6（恶魔牙本系，已 2.5 化） | 0.9 | 0.2 | 0.7 | 1/2 |
| goldflyBlade lv1~lv2（修改前） | 1.4 | 0.1 | 0.7 | 2 |
| goldflyBlade lv1~lv2（**修改后**） | **0.9** | **0.2** | 0.7 | 2（不动） |

- 修改方式：沿用第四批的**等长替换**（1.4→0.9、0.1→0.2），严格限定在 goldflyBlade 块内（偏移 160406~165203），共 **4 处、6 字节差异**（attackGap 每处 2 字节 ×2 + attackDelay 每处 1 字节 ×2）。
- 安全边界：全文件 `<attackGap>1.4<` 计数 13→11、`<attackDelay>0.1<` 计数 21→19，恰各减 2（仅黄金两级），其余武器（含普罗米修斯系 1.4/0.2、鞭炮系 1.2/0.15 等）零影响。
- `bulletNum`（黄金 lv1=2 vs 恶魔牙 lv1=1）属**每击弹数**非射速，按范围红线不动；`reduceRa` 两边本就一致（0.7）。
- 修改脚本与前后副本：`tmp-soya-family-test\flyblade-25-34-gh-audit\work\gold-fix\rate\`（config-before.bin / config-after.bin）。

## 三、构建与验证

- `构建.bat` 退出码 0，`build\game.swf` 再生（C07CAC3EF​E7904B2CBBC0CE8A6DA134DE57A986ECBC0FD359493C402C15F5517）。
- FFDec 导出 game.swf 内嵌 binaryData 回读：`6_EmbedXml_xmlClass7` 与修改后 bin **SHA-256 字节级一致**（8B6C5E514B40BB8AC310C2F115A2FF60A5ECED0173203CEB0AC006F0CC7E547E）；game.swf 内 goldflyBlade 两级实测 attackGap=0.9、attackDelay=0.2，flyBlade lv1~6 维持 0.9/0.2 不变。
- `sub1130.swf`（354DCBA6）本次未动，上一任务状态不变。

## 四、待实机确认项

1. 实机手感：黄金恶魔牙/黄金撒旦之力射速应与同级恶魔牙一致（面板值与实战值此前已验证同结构，无需重复计时，如有异议再实机计数）。
2. 黄金 lv1 每击 2 发（本系 lv1 为 1 发）为既有设计，如需对齐属数值范围扩展，另行决策。
