# 黄金恶魔牙 LV1 恢复单发 - 20260916

## 一、目标

用户实机反馈：黄金恶魔牙（lv1）现在同时发射两颗子弹，应为单发，到 lv2 才是双发；此前测试疏漏未发现。拍板 lv1 恢复真单发，与本系恶魔牙 lv1（单发）一致。

## 二、查证结论（先于修改）

对配置 bin 全部 21 个历史版本逐一提取 goldflyBlade 六级 bulletNum：

- **lv1 的 `bulletNum=2` 从仓库初始提交（ba10543，11.3 基底原始配置）就存在**，之后任何提交都未改过第一形态的 bulletNum——"之前改的时候把第一形态也改了"不成立；
- 观感变化的真正来源是 `52f929e`（双发错位补齐）：该批给两级补 `bulletTranslation=6` 前后记录过"goldflyBlade 两级 bulletNum=2 但缺字段，两弹同点重叠**观感一发实质双倍判定**"——即 lv1 的双发一直存在（双倍伤害判定），只是旧版看不出来；错位补齐后变可见，被感知为"第一形态被改成了双发"。当时文档标注"黄金 lv1 双发维持既有设计"系沿用旧口径，本次由用户裁定推翻。

## 三、实际修改与验证

- 仅 goldflyBlade 块 lv1 节点两处：`<bulletNum>2</bulletNum>`→`<bulletNum>1</bulletNum>`（恰 1 处断言）、删 `<bulletTranslation>6</bulletTranslation>`（恰 1 处断言）；lv2~lv6（2/6）与本系 flyBlade 块原样；
- 回读：黄金六级 bulletNum 序列 = **1,2,2,2,2,2**、translation ×5——与本系 flyBlade 六级**完全同构**（1/无、2/6×5）；
- 安全断言：底稿哈希 7AA24AF5、lv1 段以 lv2 name 为界切分、块外头尾不变；
- 脚本：`goldflyblade_lv1_single_shot.py`；
- 终态：config=7AA24AF5 → **51DCA719**（−40B），game.swf=76CDA7CA → **F23E399E**（内嵌 binaryData 回读字节级一致）；
- `构建.bat` 退出码 0；`--check sa` / `--check sa_debug` 双自检 0。

## 四、待实机确认项

1. 黄金恶魔牙（lv1）每次开火只有一颗金色子弹、无双发错位；
2. lv2 起恢复上下双发（±6px）；
3. 附带说明：lv1 由"隐藏双发（双倍判定）"变为真单发后，lv1 实际输出为原先观感的一半——此即用户裁定目标（与本系 lv1 对齐）。
