# 黄金恶魔牙红弹移除与圈链枪轴线校正执行总结

> 完成日期：2026-09-14
> 执行批次：黄金恶魔牙专项第一批（第五批套链后的位置/内容修正）
> 部署版本：`swf\sub1130.swf` SHA-256 前缀 **2E303889 → 354DCBA6**
> 用户确认：2026-09-14 前后对比图确认后部署并推送

## 一、目标

用户实机观察（当前渲染基准图佐证）指出黄金恶魔牙（goldflyBlade_lv1，char 391）两处问题：

1. **蓄力特效中有一颗红色飞弹**——即恶魔牙自己的红刃子弹造型，悬浮在枪口前方全程不消失；黄金版子弹为蓝白色，该红弹视觉多余。
2. **蓄力小圆圈与开火大圆圈整体位于枪水平线以下**，与普通恶魔牙（圈链压在枪管轴线上）的相对位置不符，不是理想位置。

范围口径：仅黄金恶魔牙（391）；黄金撒旦之力（399）与普通两级（897/894 等）不动。

## 二、实际修改（全部在 sprite 391 时间轴内，共 14 处）

修改前逐帧实测（证据等级 A，部署版 2E303889 swf2xml + FFDec 渲染）：

- 红弹 = **char 2711（2.5#522）**，第五批自 2.5 lv1 链 #523 带入；f2（导出帧计 f1）以 PlaceObject2 放置于 d15，矩阵含 -0.509 等比缩放＋旋转，平移 (1977,940) 缇 ≈ (99,47)px，**此后无 RemoveObject，全程可见**。全文件仅 391 与 897（普通 LV1，保留）两处引用；`placeFlagHasName="false"`，无运行时命名引用，删除无代码影响。
- 圈链偏低根因：第六批重锚对 391 全链施加 (＋88,＋79) 缇，其中**横向 +88 正确**（对齐枪口前移），**纵向 +79 为过量修正**——实测金LV1 本体（形状 365）与普LV1 本体（2.5#500，char 2689）的可见内容在各自 sprite 空间占据几乎同一绝对位置（行覆盖剖面逐行一致，y≈27.4~48.4px），链条纵向本不需移动。净效果 = 全链比理想位置低 79 缇 ≈ 4px。
- 圈系列机制备注：白圈 2652~2677 六形态共用 f1 馌矩阵（后续 5 个为 placeFlagMove 沿用矩阵，无独立矩阵可调）；烟团 2650~2683 八个各自带矩阵；电弧 2709×4（d3/6/9/12）同样被第六批 +79 影响。

执行项（隔离工作区 `tmp-soya-family-test\flyblade-25-34-gh-audit\work\gold-fix\`，脚本 `goldfix_apply.py`）：

1. **删除** 391 中 char 2711 d15 的放置（1 处）——红弹移除；
2. **ty −79 缇**（13 处）：2652 圈系列共享矩阵（639→560，六个圈自动跟随）、2709 电弧×4（855→776 ×2、830→751 ×2，随链回正避免与圈链脱节）、烟团/光球 8 处（2650:546→467、2655:510→431、2660:467→388、2665:445→366、2670:332→253、**2675 开火大光球:79→0**、2680:454→375、2683:419→340）。tx 一律不动（+88 保留）。
3. 399、897、894 及其余全部 sprite 未动（399 经逐项核对：链矩阵=普LV2+90/+542 缇，与本体 398 的 +27.1px 下移精确抵消，相对位置已与普通版一致）。

部署：FFDec xml2swf 重建全量 sub1130.swf，三处同步（`swf\`、`runtime\swf\`、`build\swf\`），manifest `config\build\current-resource-manifest.sha256` 更新为 354DCBA6…。

## 三、构建与验证

- 隔离测试版先行：修改后 XML → SWF → 重新导出 391 十一帧渲染，红弹消失、白圈/蓝圈/开火大光球/消散环全部回到枪管水平线，经用户前后对比图（`黄金恶魔牙红弹移除与圈链校正对比图-20260914.png`）确认。
- 部署后回读（swf2xml）：391 无 2711、13 处目标 ty 全部命中；897 仍含 2711（普通版未动）；399 无 2711（与改前一致）。
- `scripts\launch_game.bat --check sa` 通过（manifest 与 175 项运行资源一致）。
- 渲染基准更新：`当前渲染基准-20260912\恶魔牙金LV1_黄金恶魔牙_11帧渲染基准_20260914.png`（新），旧图移入 `历史对比图-仅批次证据-20260912\`；基准 README 来源哈希与金LV1 实测行同步更新（f4 质心 (82,55)、占枪长 62%）。
- 改前版本可回溯：git 提交 a917f9e 中的 `swf\sub1130.swf`（2E303889）。

## 四、待实机确认项

1. 实机蓄力/开火：红弹不再出现；各帧圈链贴枪管轴线（枪口朝左镜像关卡建议重点看一眼）。
2. 蓝色电弧（2709×4，2.5#520）随链上移 4px 后与金色枪体的叠观感——此前挂起项"电弧违和可摘"仍待用户实机定夺（若摘除属新决策，另行执行）。
3. 黄金撒旦之力（399）本次未动；如实机观察其圈链位置仍有异议再立项。
