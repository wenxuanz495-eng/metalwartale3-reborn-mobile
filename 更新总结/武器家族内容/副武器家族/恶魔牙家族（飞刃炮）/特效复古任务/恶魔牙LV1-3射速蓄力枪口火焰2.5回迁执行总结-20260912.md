# 恶魔牙 LV1~LV3 射速/蓄力/枪口火焰 2.5 回迁执行总结

> 执行日期：2026-09-12
> 性质：**已构建入库待实测**（三处同步＋manifest＋构建＋sa 自检通过；游戏内实测由用户进行）
> 前置：《恶魔牙家族2.5-3.4-当前版对比总结-20260912.md》（只读对比，本批范围据此确定）

## 一、目标

按用户指令，将恶魔牙 LV1（恶魔牙）/LV2（撒旦之力）/LV3（地狱之触）回迁 2.5 版本，共三项：
1. **射速回 2.5**：attackGap 1.4→0.9、attackDelay 0.1→0.2；
2. **蓄力特效回 2.5**：白色小圈脉冲序列（用户标记者）＋LV2/LV3 小点；
3. **枪口火焰回 2.5**：LV1 四层蓝色电弧（ADD）＋枪口刀刃能量帧＋本体互换；整条开火链（含烟团）随之恢复 2.5 结构。

**范围红线**：LV4~LV6、黄金恶魔牙、子弹、受击、开火音（维持 366）、本体待机贴图（LV1 像素一致不变；LV2/LV3 随整链恢复 2.5 原版涂装，差异约 11%，见"待实机确认"）。

## 二、实际修改

### 1. sub1130.swf（swf\ ＝ build\swf\ ＝ runtime\swf\ 三处同步，manifest 已更新）

- 合并脚本：`flyblade_restore_merge.py`（本目录；碎裂炮 chipped_restore_merge.py 同源机器）
- **递归闭包导入 2.5 定义 68 项**（新 ID 2644~2711，插入点在目标精灵定义之前，保持先定义后引用）：
  - 本体形状 500/502（LV1 带刃/无刃互换对）、497（LV2）、494（LV3）＋位图 450/453/456/459/.../499
  - 白圈脉冲 460/465/470/475/480/485；LV2/LV3 小点 454
  - 电弧精灵 520（17 帧内部形状 503~519）
  - 枪口刀刃能量 522（内含 521）
  - 烟团 458/463/468/473/478/483/488/491（内含 457/462/.../490）
- **三级本体时间轴整段替换为 2.5 模板**（523→897、498→894、495→891，frameCount 11 不变）：
  - 挂点引用 110→104 改绑（basePoint/shootPoint 命名实例保留；矩阵与 2.5 逐值相同）
  - StartSound 保留在 f2、soundId=366（开火音维持现状；帧位铁律相关议题见"待实机确认"）
  - 当前版共享辉光 369~390 在 LV1~LV3 的引用随之消失（角色保留，LV4~LV6/黄金继续使用，可随时恢复）
- 关键新 ID：500→2689、502→2691、460→2652、470→2662、520→2709、522→2711、454→2647

### 2. 配置 bin（decompiled\embedded-xml-assets\6_EmbedXml_xmlClass7_...bin）

- **等长字节替换**（38EA3C12 → 58CC22AC，文件长度不变）：
  - flyBlade 块内**前三级**（以第 4 个 `<armsLevel>` 为界）`<attackGap>1.4→0.9`、`<attackDelay>0.1→0.2`
  - 第 4~6 级逐字节保留 1.4/0.1；块外全部资源零改动

### 3. 部署与构建

- swf\sub1130.swf ← 合并产物；manifest `config\build\current-resource-manifest.sha256` sub1130 条目 → `8B903B3A...`
- 构建.bat 全量通过（server/launcher/build_swf/prepare_runtime/产物校验，175 资源）

## 三、构建与验证（证据链）

| 步骤 | 结果 |
|---|---|
| SymbolClass 回读 | flyBlade_lv1~lv6=897/894/891/888/885/882、goldflyBlade=391/399、bullet=878 全部不变 |
| **渲染逐像素比对** | 新 897/894/891 全部 11 帧 vs 2.5 原帧（523/498/495）：**逐像素一致**（画布尺寸亦一致 183×110/161×80/165×80） |
| 未动范围回归 | lv4（888）/lv6（882）/黄金 lv1（391）/子弹（878）渲染与合并前逐字节一致 |
| 配置 BinaryData 回读（build\game.swf） | lv1~3 attackGap=0.9 ×3、attackDelay=0.2 ×3；lv4~6 attackGap=1.4 ×3、attackDelay=0.1 ×3 —— PASS |
| 三处同步 | swf\ = build\swf\ = runtime\swf\ = `8B903B3A...` |
| 构建与自检 | 构建.bat 退出码 0；`launch_game.bat --check sa` 通过 |
| 哈希 | sub1130=`8B903B3A05AFF51D26EE356CA55F1F3B765BFAE645C20A460EF17D560F880D3A`；config bin=`58CC22AC789EE9AE88E4B9FA92FE25C0CF3229754B9B9313A3D9AFD655DA0C3E`；game.swf=`5F3D3DDB0A8C2E0FD7521392270AFCD22ABDDD0B2E9EF8F831D53F7F49CB7D8B` |

过程勘误（已修复）：①首版闭包误用"直接引用"导致位图/精灵内部形状缺失（本体渲染成红块占位）——改为**递归闭包**后 68 项完整；②导入源遍历误写为 GH tags（会误改 GH 同 ID 自有角色）——断言"导入数不符"拦截后改为遍历 2.5 tags。两处修复均已固化进脚本。

## 四、待实机确认项

1. **蓄力观感**：待机→开火前，枪身小圈脉冲（LV1 在尾部圆舱，LV2/LV3 在本体原点）是否与记忆一致；
2. **枪口火焰**：LV1 蓝色电弧环＋刀刃发射帧、LV2/LV3 的开火表现是否恢复 2.5 观感；
3. **射速**：attackGap 0.9＋attackDelay 0.2 的实际射速手感（面板≠实战）；
4. **LV2/LV3 待机涂装**：随整链恢复 2.5 原版（红刃色阶与当前版有约 11% 差异）——若要"新涂装待机＋2.5 开火链"的混合方案，第二轮可拆；
5. **开火音**：维持现状 366 且挂 f2（帧音效铁律：playOnce 落点帧不触发）——若实机发现无开火音，与回迁无关（回迁前同结构），可另立项按碎裂炮方案克隆挂 f4；
6. LV4~LV6、黄金、子弹、受击应无任何变化（渲染回归已证，实机复核）。

## 五、回滚

- sub1130：备份 `备份-回迁前配置与资源-20260912\sub1130.swf`（ECCEB0C9）覆盖回三处＋manifest 回写 ECCEB0C9；
- 配置：备份同目录 `6_EmbedXml_xmlClass7.bin`（38EA3C12）覆盖回 decompiled 后重新构建。
