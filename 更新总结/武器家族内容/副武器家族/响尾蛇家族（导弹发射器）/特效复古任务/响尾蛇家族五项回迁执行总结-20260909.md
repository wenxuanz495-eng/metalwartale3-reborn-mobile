# 响尾蛇家族（导弹发射器）五项回迁执行总结

> 日期：2026-09-09
> 状态：✅ **已实机确认定稿**（2026-09-09 用户实测通过，五项无问题反馈；禁区字段维持零改动）
> 依据：`响尾蛇家族2.5-3.4-11.3三版本对比总结-20260909.md` + 用户五项决策
> 执行范围：①射速恢复 ②枪口焰六帧序列恢复 ③lv2~lv4 开火闪光层恢复（最高优先）④受击特效恢复 ⑤开火音效恢复
> **禁区（用户明令）**：成长线与经济字段（commonLevel / reduceRa / price / mustItems / mustLevel）一律未动。

## 一、目标

把 2.5 版导弹发射器家族（`arms id="missile"`，响尾蛇/眼镜蛇/巨蝮蛇/八歧大蛇）的五项战斗观感差异回迁到当前 GitHub 版（11.3 原型），同时严格保持经济与成长体系不变。

## 二、实际修改

### 2.1 `swf\sub1130.swf`（= `build\swf\` = `runtime\swf\` 三处同步）

新 SHA-256：`78979D074279E33698BABBB3E8A642FCF5A82A301790B936A2A044A326789370`（旧 `cea95884…` 已备份）。

**新增定义 33 项（新 ID 区间 1684–1716）**：

| 类别 | 内容 | 新 ID |
|---|---|---|
| 闪光层（②③核心） | lv2/lv3/lv4 前段闪光覆盖层 精灵+纯矢量形状（克隆自 2.5 #1407/1406、#1402/1401、#1390/1389） | 1685/1686、1687/1688、1689/1690 |
| 受击爆炸（④） | 六帧爆炸形状 ×6 + JPEG3 位图 ×6（2.5 #1657~#1668）+ 爆炸精灵（2.5 #1678） | 1690~1695、1684 组、1702 |
| 枪口焰 wrapper（②） | lv1 六帧形状（2.5 #1419~#1424）、lv2 三帧（#1409/#1411/#1413）、lv4 三帧（#1392/#1394/#1396），位图填充重映射至 sub1130 现存焰图 | 1703~1708、1709~1711、1712~1714 |
| 音效 | 开火音效（2.5 sound 111 全数据）、受击爆炸音（2.5 sound 1673 全数据） | 1715、1716 |

**时间轴重建**：四级本体（#1398/#1393/#1386/#1383）subTags 整段替换为 2.5 原时间轴（#1425/#1415/#1403/#1398），角色/音效 ID 重映射，挂点矩阵强制为当前值（两版本就逐字节相同）：
- 枪口焰恢复 f4~f11 六帧渐进序列（lv1 焰效在 d2，lv2~lv4 在 d4，与 2.5 深度一致），f11 清屏；
- lv2~lv4 闪光层挂回 d2 @(21,636)/(20,672)/(20,788)，逐帧 alpha 呼吸脉冲（lv2/lv3：0→85→171→**identity+ADD(开火帧闪白)**→…；lv4：36→109→183→**满亮**→…，波峰精确对齐 f4/f10 开火帧）；
- 四级 StartSound 改指新开火音效 1715（f4 触发，与 2.5 相同帧位）。

**SymbolClass 死名改绑（④）**：`positron_hit_effect` 由 824（旧 positron 遗留孤儿）改绑至 1702 新爆炸精灵。改绑对象经三级验证：全配置 bin 零活引用（排除注释）、sub1130 内部零 PlaceObject 引用、decompiled 全脚本零命中；并明确排除 `motion_hit_effect_1~4`（运行时随机后缀可解析到，禁止改绑）。

**共用资产保护（未动证明）**：
- `boom_hit_effect`（#1454）被敌方 XML 178 处 + 主武器 XML 17 处共用 → 未原位重建，走死名改绑；
- 开火音效 108 被 zhongzifeidan_lv1/lv2、killPig_lv1 共用 → 未原地换数据，走新增音效 ID；
- `bullet.swf` 未动（现行四色 boom 属 3.4 官方美术体系，保留于文件内）。

**孤儿说明**：lv1 旧焰效件 1397/112/114 替换后仍被其他精灵引用（守卫检查 refs>0），按规保留未删。

### 2.2 `decompiled\embedded-xml-assets\6_EmbedXml_xmlClass7_….bin`（+22 字节）

- `<attackGap>0.9→0.4`（missile 块内 ×4，等长替换）；
- `<hitImgLabel>` ×4：`bullet/yellow_boom`/`red_boom`/`blueness_boom`/`green_boom` → `sub/positron_hit_effect`；
- **禁区字段零改动**（断言校验：commonLevel 8/14/19/29、reduceRa 0.9、price、mustItems 均原值）。

### 2.3 `config\build\current-resource-manifest.sha256`

`swf\sub1130.swf` 行更新为新哈希 `78979D07…`。

## 三、构建与验证

| 验证项 | 结果 |
|---|---|
| FFDec xml2swf 重建 | 退出码 0，sub1130-restored.swf 3,559,701 字节 |
| swf2xml 回读结构比对 | 四级本体 13 帧；lv1 焰序 f5~f10 六放置+f11 清屏；lv2~lv4 d4 七放置（f4 开火体+六焰）；闪光层 12 条 alpha/blend 记录（f4/f10 ADD，f7/f13 blend1）；爆炸精灵 1702 七帧六放置；StartSound 1715×4 / 1716×1 |
| 合并脚本内置断言 | 导出表仅 positron_hit_effect 一处变化；无重复 spriteId；新 ID 1684~1716 无冲突；缺失闭包=0 |
| 三处同步 | swf\ = build\swf\ = runtime\swf\ 哈希一致 `78979D07…` |
| manifest | 已更新并通过 prepare_build_runtime 校验 |
| 构建.bat | 退出码 0，175 项运行时资源就绪，核心产物校验通过 |
| BinaryData 回读（构建产物 game.swf） | missile 块 attackGap=`0.4`×4、hitImgLabel=`sub/positron_hit_effect`×4；**commonLevel/reduceRa/mustItems 原值未动**；源 bin 与构建产物 missile 块逐字节一致 |
| 启动自检 | `launch_game.bat --check sa` 与 `--check sa_debug` 均通过（退出码 0） |

视觉证据（本目录）：响尾蛇lv2开火恢复前后对比图、响尾蛇开火闪光恢复前后对比图（lv2/lv4 f4 白热帧已与 2.5 一致）、响尾蛇受击恢复后六帧对比图。

## 四、实机确认结论（2026-09-09）

用户实机验证五项全部通过，无问题反馈：
1. 射速——0.4 秒攻击间隔实战节奏正常；
2. 开火音效——2.5 原音听感确认；
3. 闪光层——lv2/lv3 ADD 闪白与 lv4 全亮脉冲实机观感确认，与攻击间隔同步正常；
4. 枪口焰——六帧渐进序列连贯；
5. 受击特效——六级统一 2.5 像素爆炸+专属爆炸音接受。

家族状态：**✅ 已实机确认定稿，收口**。

## 五、产物与回滚

- 回滚：用 `备份-被替换的旧资源-20260909\` 内 sub1130旧.swf / 6_EmbedXml_xmlClass7旧.bin 覆盖三处同步位置与源 bin，manifest 行还原 `cea95884…`，重跑构建。
- 过程产物：`tmp-soya-family-test\rattlesnake-audit-20260909\work\`（合并脚本 rattlesnake_restore_merge.py 已随档入本目录）。
