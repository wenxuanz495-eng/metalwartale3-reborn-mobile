# 鞭炮家族 2.5 回迁执行总结（含黄金鞭炮）

> 执行日期：2026-09-10
> 性质：✅* **已构建入库待实测**
> 批准范围（用户指令）：**两家族全部形态**——鞭炮 lv1~lv4 + 黄金鞭炮 lv1~lv2；①待机/开火特效/枪口火焰回 2.5 ②子弹回 2.5 ③受击特效保留**新版鞭炮的** `bullet/green_boom`（黄金鞭炮由 `sub/cutter_hit_effect` 改绑为 `bullet/green_boom`）。
> 前置：`鞭炮家族2.5与当今版美术对比总结-20260910.md`（🔍 只读对比）。

## 一、目标

按用户批准范围，将 2.5 鞭炮的待机本体（lv1）、蓄力/开火/枪口火焰分层动态（全家族）、8 帧绿色火花子弹（全家族共用弹）回迁至当前版 sub1130.swf；受击按决策保持/统一为新版鞭炮受击。

## 二、实际修改

### 1. sub1130.swf（FFDec XML 合并，脚本 `banger_family_restore.py`）

- **2.5 依赖闭包导入**：以 2.5 `banger_lv1`(char259) + `banger_lv1_bullet`(char228) 为根，闭包 **41 项**（待机本体 shape230、光点六态 232~243、ADD 光爆 sprite244、挂点 sprite110、火力帧 246~258、子弹四形状 221/223/225/227 等），全量重映射为**新 ID 2509~2549**（全局定义最大 2508 +1，含 sound 命名空间，规避纳米号撞号教训）。**声音不导入**。
- **banger_lv1 (651)**：subTags **整段替换**为 2.5 char259 时间轴（25 帧不动）——f1 待机绿色本体+挂点(新110, depth15/17)，f2~f8 光点级联蓄力，f7 起 ADD 层(blend=8)+颜色变换脉冲，f9~f15 枪口光球/长喷射/余辉（depth1 本体之下），f14~f17 清场；`StartSound 237 → 125`（两版字节级一致，免导入，sha256 `3F357B81…`）。
- **banger_lv1_bullet (654)**：subTags 整段替换为 2.5 char228（**frameCount 1→8**，四形状交替闪烁）。
- **banger_lv2 (641) / lv3 (631) / lv4 (622) / Goldbanger_lv1 (150) / lv2 (140)**：保留各自本体与挂点（depth3/5，f1 安置持续全帧），**剥离现烘焙开火段**（f2+ 非挂点 PlaceObject/RemoveObject 全部移除，旧火力形状定义保守保留不删），插入 2.5 火力轨道（**depth+20**，帧 2~19，同测试包 `banger-gold-restore` 先例）。
- SymbolClass 253 项不变；7 个导出名与 Character ID 映射全部不变（651/654/641/631/622/150/140）。

### 2. 副武器配置 `6_EmbedXml_xmlClass7_...bin`（纯文本块级补丁）

- **仅 Goldbanger 块**：lv1/lv2 `hitImgLabel` `sub/cutter_hit_effect` → `bullet/green_boom`（恰好 2 处，块内断言校验；全文件该串共 25 处、其余 23 处属 8 个其他家族未动）；字节数 **−8**。
- 鞭炮 lv1~lv4 受击保持 `bullet/green_boom`（无改动）；两家族 `imgLabel`/`bulletImgLabel` 等全部字段未动（回迁经同名导出整段替换实现，**无需配置改绑**）。

### 3. 部署（三处同步 + manifest + 构建）

- `swf\sub1130.swf`（源）→ **F8B29D8EC9562611A4A96457FB73CA1CD7B70008415CF1A4201FBFD68425A0D4**；`build\swf\`、`runtime\swf\` 三处一致（runtime 为构建后手动补齐，构建脚本只管 build\swf）。
- `config\build\current-resource-manifest.sha256` sub1130 行已更新为新哈希。
- `构建.bat`（实际执行 `scripts\build_all.bat`）：**退出码 0**——server/launcher 重编、game.swf 重打包（config bin 经 BinaryData 补丁清单第 6 行嵌入）、runtime 175 资源就绪。首次因残留 server.exe 锁文件失败（拒绝访问），按仓库惯例 taskkill 后重试通过。

## 三、构建与验证

| 检查 | 结果 |
|---|---|
| xml2swf 重建 | 退出码 0，`output/sub1130-new.swf` 4,464,302 B |
| swf2xml 回读结构 | **PASS**——651：25 帧、ADD(blend=8) 在、音效 125、f1 depths=2/15/17、引用全部为 2509+；654：frameCount=8、8 帧、4 形状；六个叠加时间轴：25 帧、donor 字符≥10、旧火力字符 0 残留、depth20+ 存在、挂点 3/5 在 |
| SymbolClass 回读 | 253 项完整，7 个相关导出名映射不变 |
| **像素级验证** | **651 全 25 帧 vs 2.5 char259 = 0.0% 差异；654 全 8 帧 vs 2.5 char228 = 0.0% 差异**（内容对齐法） |
| 金色系渲染抽查 | 150 f7 蓄力光点+ADD 辉光、f11 绿焰喷射，金色本体保留正常 |
| game.swf 内嵌配置回读 | BinaryData #6 中 `green_boom` 受击 6 处（鞭炮4+黄金2）、`cutter_hit_effect` 23 处（他家族原样） |
| 产物哈希 | sub1130=**F8B29D8E**（三处）、config bin=**9E15CD3B**（−8B）、game.swf=**3DEB126C** |
| 启动自检 | `launch_game.bat --check sa` 退出码 0；`--check sa_debug` 退出码 0 |

## 四、待实机确认项

1. lv1 全套 2.5 观感：蓄力点列→白热闪光→长喷射节奏与 2.5 原版手感；
2. 五个叠加形态（lv2~lv4/黄金×2）的 2.5 火力层为 depth20+ **在炮体前方**渲染（2.5 原版火力帧在本体之后），焰团遮枪口程度是否可接受；
3. ADD 层（s244 内嵌 2.5 绿色本体形状）叠加在金色/红色本体上的绿色辉光观感；
4. 新 8 帧子弹的实机飞行观感与命中判定（`bulletWidth=10` 未动）；
5. 黄金鞭炮受击由激光痕改爆炸后的实机观感；
6. 开火音（f4，与旧版同一音频）触发是否正常。

## 五、文档与证据索引

| 文件 | 内容 |
|---|---|
| 鞭炮回迁前后对比图-20260910.png | lv1 与黄金 lv1 回迁前后关键帧对比 |
| 鞭炮家族2.5与当今版美术对比总结-20260910.md | 前置只读对比 |
| 备份-被替换的旧资源-20260910/ | 旧 sub1130（B9C46C9D）、旧 config bin（A08B4066）、说明 |
| 工作区 `tmp-soya-family-test\banger-family-25-restore\` | 输入哈希、合并脚本、merged.xml、回读校验、逐帧渲染 |
