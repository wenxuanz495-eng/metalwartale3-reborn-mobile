# 战殇家族（激光棱束炮）射速 2.5 回迁执行总结

> 执行日期：2026-09-09（副武器第四家族）
> 用户决策范围：**仅射速回到 2.5**（attackGap 2.4→1.5）；受击特效维持现状（3.4/GH 三态），`bulletLink` 死配置保留不动
> 前置审计：《战殇家族2.5-3.4-11.3三版本对比总结-20260909.md》（同目录）
> 工作区：`D:\superalloy\tmp-soya-family-test\laser-family-audit\`（补丁脚本 `laser_config_patch.py` 已随归档入库）

## 一、目标

把战殇家族（`arms id="laser"`，四级 战殇/破邪/星痕/无幻）的射速恢复为 2.5 原版：`attackGap` 2.4→1.5（2.5=3.4 均为 1.5）。

## 二、实际修改

**配置**：`decompiled\embedded-xml-assets\6_EmbedXml_xmlClass7_EmbedXml_xmlClass7.bin`（脚本 `laser_config_patch.py`，同目录）

- laser `<arms>` 块内 `<attackGap>2.4</attackGap>`→`<attackGap>1.5</attackGap>` ×4（等长替换，bin 总大小不变 291751 字节）
- 块外零变化断言通过；块内替换后无 2.4 残留
- bin 哈希：`4B186DF335626C28`（elastic 回迁后版）→ `CFE4B278ECEF35A9`

**不涉及**：`swf\sub1130.swf` 本家族零差异未动；受击特效（3.4/GH 三态）按决策保持；`bulletLink=missile_lv1` 死配置（无 index 属性，运行时不触发）保留；经济/成长字段零改动。

## 三、构建与验证

| 验证项 | 结果 |
|---|---|
| 构建 | `构建.bat` 退出码 0，175 运行时资源校验通过，`build\game.swf` 生成（C31A669A8DBB7400…） |
| BinaryData 回读 | laser 块 `attackGap 1.5` ×4、`2.4` 残留 0；顺带复核 elastic 回迁完好（0.4 ×4、`sub/positron_hit_effect` ×4） |
| 双自检 | `launch_game.bat --check sa` 退出码 0；`--check sa_debug` 退出码 0 |

## 四、待实机确认项

1. 射速实战计时：attackGap 1.5 的实际发/秒（面板≠实战；对比截图面板 1.2 发/秒与两版配置均不直接对应，实机核对口径）。
2. 受击三态为决策保持项，若日后想回 2.5 五态，按对比总结第六节小成本方案另行立项。
