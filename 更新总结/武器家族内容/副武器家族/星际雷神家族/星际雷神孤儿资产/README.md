# 星际雷神孤儿资产（20260919 移出存档）

> 本文件夹是**无人打扰的隔离存档**：V54（2026-09-19）从 `swf\sub1130.swf`（81C2B7F3 → 3CEFC59F）移除的孤儿定义，全部在此留档，**可随时按本文方法回植**。
> 移除裁定：用户 2026-09-19（"孤儿资产放到一个无人打扰的文件夹"）。

## 内容清单

| 文件 | 身份 | 来源与历史 |
|---|---|---|
| `bitmap2902-旧MK2本体(V1).png` | 位图 2902（103×47） | V1 时代的旧 MK2 本体贴图，被现役 2934/2935（部件1）取代 |
| `bitmap2936-星际雷神特效1(V18弃用).png` | 位图 2936（145×67） | V18 误判期导入的 lv2 蓄力特效（现行=新特效1~5），V19 撤回 |
| `bitmap2938-星际雷神特效2(V18弃用).png` | 位图 2938 | 同上（特效2） |
| `bitmap2940-星际雷神特效3(V18弃用).png` | 位图 2940 | 同上（特效3） |
| `bitmap2942-星际雷神特效4(V18弃用).png` | 位图 2942 | 同上（特效4） |
| `定义存档-可回植.xml` | 全部被移除定义的 XML 原文 | 2902 位图 + 2903 形状（旧本体对）；2936~2943 位图+形状（特效对，原嵌于 sprite 100 内部） |

- 源 PNG（画师原件）：`相关素材\雷神mk2\星际雷神.png`（旧本体）与 `相关素材\雷神mk2\星际雷神特效1~4.png`（弃用特效）——留在原处未动。
- sprite 100 内另有一个原版遗留流声头，未移除（无法判定是否被运行时使用，保持最小改动）。

## 回植方法

1. FFDec 打开当前 `swf\sub1130.swf` 导出 XML；
2. 将 `定义存档-可回植.xml` 内的 10 个 `<item>` 按存档注释插回原位（2902/2903 为顶层定义，2936~2943 插回 sprite 100 的 subTags 内、PlaceObject 2905 之前）；
3. xml2swf 回读 → 三处同步 → manifest 更新 → 构建（注意：重制后如需挂到时间轴，还须按时间轴替换失效红线用 RO+add 放置，并同步更新 `贴图索引.md`）。
