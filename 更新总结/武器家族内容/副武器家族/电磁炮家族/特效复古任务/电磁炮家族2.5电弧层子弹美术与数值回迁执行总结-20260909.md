# 电磁炮家族 2.5 电弧层/子弹美术/数值回迁执行总结

> 日期：2026-09-09
> 性质：**已执行回迁，并已实机确认通过**（2026-09-09 用户实测确认回迁全部成功；受击特效按决策维持现状不回迁）。
> 范围：①本体电弧动画层（四级）②子弹逐级独立美术（含 lv3/lv4 双层）③lv1 射速 ④后坐力。
> 隔离工作区：`D:\superalloy\tmp-soya-family-test\elecgun-restore-20260909\`（合并脚本 `elecgun_restore_merge.py`、双侧 XML、回读件、清单）。

## 一、目标

按 20260908 检查审计与 20260909 可行性研究的结论，将 2.5 版电磁炮家族的标志性视觉（枪体电弧缠绕动画、逐级独立大弹）与 lv1 射速/后坐力手感回迁至当前版；受击特效按用户实测决策保持 `bullet/purple_motion` 现状。

## 二、实际修改

### 1. `swf\sub1130.swf` ＝ `build\swf\sub1130.swf` ＝ `runtime\swf\sub1130.swf`（三处同步，SHA-256 `cea95884009a2a1f…`，原 `b8854b9a…`）

- **本体四级时间轴重建**（整树克隆 2.5 结构）：elecGun_lv1 7→**13 帧**、lv2/lv3/lv4 7→**14 帧**；每级插入 **12 个逐帧 ADD（blendMode=8）电弧放置**（每帧一张独立闪电图：精灵→形状→位图三层闭包）；替换掉原 2 次矩阵位移抖动帧。
- **本体位图未动**：仍用当前角色 1416/1413/1410/1407（已证与 2.5 逐字节相同）。
- **挂点未动**：basePoint/shootPoint 实例（角色 104）保留，**标记矩阵强制覆盖为当前值**（lv1 565,380/1200,380 等），保证子弹出射坐标零漂移。
- **子弹四级替换**：elecGun_lv1_bullet＝2.5 单层大弹（54×44）；lv2_bullet＝60×48；lv3_bullet＝**双层**（核心＋ADD 光晕）；lv4_bullet＝**双层**（69×51＋79×59）。结构逐项对照 2.5（放置数 1/1/2/2、混合模式、矩阵）。
- **导入闭包 161 项**（新角色 ID 1523~1683，定义先于引用插入）；**移除孤儿资源**：旧共用子弹位图 1399＋形状 1400/1402（无任何引用、无导出名绑定，原件已备份）。
- 挂点标记（红色方格+十字）按通用规则**不回迁、实机两版均被 SWFLoaderManager 隐藏**。

### 2. `decompiled\embedded-xml-assets\6_EmbedXml_xmlClass7_….bin`（副武器配置）

- elecGun 块内 lv1 `attackGap` **1.4→0.9**（lv2-4 本就 0.9，未动）；四级 `recoilValue` **6→4**。
- 同长字节替换（3↔3、1↔1 字节），块内限定，长度不变，无残留。
- 受击调用 `bullet/purple_motion` ×4 **保持不变**（用户决策）。

### 3. `config\build\current-resource-manifest.sha256`

- sub1130 行哈希 `B8854B9A…` → `CEA95884…`。

## 三、构建与验证

- 合并脚本自检：导出表 253 项逐条一致、无重复 spriteId、闭包零缺失、新 ID 区间与既有定义零冲突。
- xml2swf 重建 + swf2xml 回读：0 SEVERE；四级本体帧数/电弧数/标记矩阵、四级子弹放置数/混合模式全部 OK（详见工作区 `work/sub1130-roundtrip.xml`）。
- `构建.bat`：退出成功，175 项运行资源校验通过，核心产物校验通过；`build\game.swf` SHA-256 `21690de3…`。
- **BinaryData 回读**：build\game.swf 内 elecGun 块 attackGap 0.9 ×4（含 lv1）、1.4 残留 0；recoilValue 4 ×4、6 残留 0；purple_motion ×4 未动。
- `launch_game.bat --check sa` 与 `--check sa_debug` 双自检通过。
- 备份：`备份-被替换的旧资源-20260909\`（原 sub1130.swf＋原配置 bin＋回滚说明）。

## 四、实机确认（2026-09-09 用户实测）

**全部通过**：电弧动画循环、lv1 射速手感、子弹（含 lv3/lv4 双层光晕）观感、后坐力幅度均符合预期，回迁定稿。

## 五、提交状态

代码与总结已同批提交推送至 GitHub 远端（规则：总结与代码同一次推送）。
