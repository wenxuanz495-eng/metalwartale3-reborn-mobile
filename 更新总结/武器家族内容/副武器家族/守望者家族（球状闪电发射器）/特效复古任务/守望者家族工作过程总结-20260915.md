# 守望者家族 · 工作过程总结（2026-09-15）

> 本文档是 2026-09-15 守望者家族两个任务线（前置事故恢复 + LV4/LV5 新加碎屑特效）的**全程工作过程记录**，供回溯与交接。
> 任务分文件夹管理：**复古任务**见 [`特效复古任务\`](特效复古任务/)，**新加特效**见 [`LV4LV5碎屑三帧-画师素材分析-20260915\`](LV4LV5碎屑三帧-画师素材分析-20260915/)。

## 一、任务线 A：两项已入库改动静默回退的取证与恢复（复古任务侧）

### 1.1 发现

开工 LV4/LV5 碎屑导入前做底稿结构分析，发现资源现状与文档记录不符：9 月 9 日已拍板建库的 **shootPoint 右移 30px ×5** 与**开火音效 f3→f5 ×5** 两项改动不在当前资源中（shootPoint translateX 为回退前旧值、StartSound 在 f3 而非 f5）。

### 1.2 取证结论（完整证据链见 [bug 维护记录](../../bug%20维护/3.0前瞻版本bug维护/守望者两项已入库改动被炽天使导入静默回退-20260915.md)）

- 09-09 23:22 炽天使审计工作区转储的合并底稿 `sub_GH.xml` 早于当晚并行落盘的两项改动（23:25+），脚本注释声称"转储自 8D885B69"与事实不符（冷备底稿实测为旧值）；
- 09-10 00:21 `c46fe23` 补交后 git 内为完好态（实测五级 1632/2000/2288/2642/2642 + f5 全对）；**03:17 `014dc62`（炽天使入库）为第一个坏提交**，其后 73 个提交未再触碰守望者本体，坏值延续；
- 丢失范围仅此两项（五级本体归一化结构 diff 证明），配置层/脚本层/LV1~3 回迁成果全部完好；
- 两道防线失守原因：回归断言粒度不足（不覆盖矩阵数值与音效帧号）、manifest 哈希链无法感知内容回退。

### 1.3 恢复执行

- `lightningball_lost_restore.py`（[特效复古任务\](特效复古任务/)）：当前底稿新鲜转储 + manifest 哈希断言 → 五级 shootPoint 矩阵整体拷贝自完好态（8D885B69）→ StartSound 移 f5 → 回读断言五级本体与完好态**结构全等**、其余 538 精灵零变化；
- sub1130 = 2F422962 → 三处同步 + manifest + 构建 0 + 双自检 0；记录入 [bug 维护](../../bug%20维护/3.0前瞻版本bug维护/守望者两项已入库改动被炽天使导入静默回退-20260915.md)；
- **流程沉淀**：①合并类脚本 TGT 底稿必须合并前现转储并断言来源哈希 == manifest（本次事故直接根因即底稿比并行改动旧 3 分钟）；②回归断言需覆盖数值级细节；③执行总结声称哈希应与提交 blob 实测核对。

## 二、任务线 B：LV4/LV5 新加碎屑特效（新加特效侧）

### 2.1 交付素材验证

画师交付 8 张 PNG（`相关素材\守望者\`，每级枪体基准 1 张 + 碎屑**三帧动画** 特效1/2/3）：互相关实测交付图与 SWF 渲染**几何 1:1**（裁剪原点 LV4 (0,12)、LV5 (0,14)），碎屑亮点与枪体亮部（蓝灯珠/黄部件/装甲反光位）严丝合缝。

### 2.2 结构分析（导入前）

- 五级导出名映射：lv1=1327 / lv2=1323 / lv3=1313 / **lv4=1307 / lv5=1304**；
- LV1~3 辉光层实测：depth2 单帧精灵内含单个形状，f1 放置 α=0，**f2 起可见闪烁**，脉冲跨约 10 帧（f3 80% → f4 ADD 闪白 → … → f7 全隐 → f8~f10 第二脉冲 → f11 收尾 → f12 隐），带逐帧 GLOW 滤镜；
- LV4/LV5 现状：无碎屑层，火焰占 depth2（f5~f11）；五级时间轴 12 帧同构 → **结构可直接套用**（火焰 d2→d4 + depth2 挂碎屑）。

### 2.3 第一轮导入（静态三帧）

画师三帧对位 f3/f4/f5（用户定案），静态放置 + f6 清场；18 项新 ID 2714~2731（位图为预乘 ARGB Lossless2——字节序与预乘均经位图 29/2714 逐像素对照实证；形状克隆文件内 shape 30 的位图 1:1 矩形包裹先例 fillStyleType=67）；火焰 d2→d4，f12 原清除标签随火焰改指 d4（回读断言抓出）。sub1130 = 17880730。

### 2.4 第二轮修订（用户实机反馈：静态三帧观感远弱于 LV1~3）

用户录屏确认"守望者的发光不止 3 帧"——LV1~3 碎屑实际跨全周期脉冲。用户拍板**全套复刻 LV1**：碎屑 f1 占位常驻至 f12，逐帧克隆 LV1 的 alpha 档（0/119/205/ADD/205/119/0/119/205/ADD/166/0）、f4/f10 ADD 闪白峰值与 GLOW 滤镜（逐元素全等断言 PASS），三图按强度档轮换（弱=特效1 / 强=特效2 / 峰值=特效3，第二脉冲复用），核心对位 f3=特效1/f4=特效2/f5=特效3 保持。sub1130 = **4784C902**（当前值）。

## 三、当前状态

| 项 | 状态 |
|---|---|
| 复古任务（全项 2.5 回迁 + 两项后续改动 + 事故恢复） | ✅ 定稿入库（sub1130=2F422962 阶段成果，含于当前 4784C902） |
| 新加特效（LV4/LV5 全周期脉冲碎屑） | ✅ 已构建入库（sub1130=**4784C902**），**待实机确认** |
| 电弧音 ×4 音量实验 | 仍生效，待实机判定 |

## 四、文件地图（本轮重组后）

```text
守望者家族（球状闪电发射器）\
├── README.md                          家族档案（本索引）
├── 守望者家族工作过程总结-20260915.md  本文件
├── 特效复古任务\                       2.5 复古回迁任务全部材料（20260909 起）
│   ├── 守望者家族回迁工作流程总结-20260909.md   九步标准流程（家族模板）
│   ├── 守望者家族全项2.5回迁执行总结-20260909.md（先读）
│   ├── 守望者家族2.5-3.4-11.3对比审计-20260909.md
│   ├── 电弧音音量提升实验-20260909.md
│   ├── lightningball_config_restore / restore_merge / shootpoint_shift / sndf5 / lost_restore .py
│   ├── 备份-被替换的旧资源-20260909\
│   └── 对比图/证据图 13 张（三版本逐帧、光晕、受击、子弹、开火模拟等）
└── LV4LV5碎屑三帧-画师素材分析-20260915\   新加特效任务全部材料
    ├── 素材交接-画师-20260909\（+ .7z）      画师交接包（辉光机制说明/参考组/交付要求）
    ├── LV4LV5碎屑三帧导入执行总结-20260915.md（含修订轮记录）
    ├── lightningball_debris3_import.py / lightningball_debris3_pulse.py
    ├── README.md                            分析结论与验证口径
    └── 对位预览/交付对比/12 帧实渲染图 14 张
```
