# 粒子刃炮家族 蓄力回迁执行总结

> 执行日期：2026-09-11
> 性质：✅* **已构建入库待实测**
> 批准范围（用户指令）：**蓄力特效回到旧版本（2.5）**——采用整段时间轴替换（2.5 蓄力层与本体同时间轴，待机美术同步回归 2.5 版；两版本待机设计几乎相同，f1 差异仅 10.09%）。
> 前置：`粒子刃炮家族2.5-当前对比总结-20260911.md`。

## 一、目标

粒子刃炮（`arms id="skullHeads"`，唯一形态黑寡妇）当前版缺失蓄力特效（f2~f8 逐字节静止），将 2.5 的蓄力辉光层随本体时间轴整段回迁，恢复蓄力前摇；开火音维持两版同内容不换。

## 二、实际修改

### 1. `swf\sub1130.swf`（FFDec XML 合并，脚本 `widow_restore.py`）

- **2.5 闭包导入 33 项**（本体 264＋蓄力辉光层 232/234/236/239/241/243 跨 depth2~11＋火焰 246/248~258/265＋挂点 110×2），全量重映射为**新 ID 2612~2643**（全局最大 2611+1）。
- **同名导出整段替换**：`skullHeads_lv1` (674) ← 2.5 #266 时间轴（frameCount 25→25 不变）——蓄力辉光恢复＋枪口火焰同步回归 2.5 细节。
- **开火音零克隆**：2.5 #237 与当前 #279 内容字节一致（sha 149ced17cca0a542，6762 字节）——导入时间轴内 StartSound 直接改挂现有 **#279**，不导入重复音。
- SymbolClass **253 项不变**；`skullHeads_lv1`(674)/`skullHeads_lv1_bullet`(657) 映射不变——**配置零改绑**。
- 子弹（657）、受击（`sub/cutter_hit_effect`→#1509）均未动（子弹按用户决策保留当前版；受击同名异内容另立项）。

### 2. 部署（三处同步 + manifest + 构建）

- `sub1130.swf`=**ECCEB0C9**AA70009FFEA78B67E7949CD75DE934020FB3FEFA6DC2DC6974276DD0——`swf\`、`build\swf\`、`runtime\swf\` 三处一致。
- manifest sub1130 行已更新。
- `构建.bat`：**退出码 0**（首次因自检残留 server.exe 锁定失败，taskkill 后通过——错误收集 4.4 既有条目）。game.swf=**1696FF25**（与星爆终态相同——本任务无配置改动，内嵌配置不变，符合预期）。

## 三、构建与验证

| 检查 | 结果 |
|---|---|
| xml2swf 重建 | 0 SEVERE，产物 4,683,595 B（合理） |
| SymbolClass 回读 | **253 项**；skullHeads_lv1=674、bullet=657 不变；highEnergy/positron/chipped 抽查原样 |
| **像素级验证** | **回迁后 674 vs 2.5 #266：25/25 帧 0.00%**——蓄力辉光（f2~f8 爬升 2410→2514）恢复，开火火焰同步回归 2.5 细节 |
| 蓄力回归判定 | 新 f2~f7 单调爬升 ✅（旧当前为 2437×7 全静止） |
| 音频 | StartSound 挂现有 #279（内容＝2.5 #237，零克隆零新增） |
| 内嵌配置 | game.swf 内嵌 xmlClass7 中 skullHeads_lv1=2 处原样（配置本任务零改动） |
| 产物哈希 | sub1130=ECCEB0C9（三处）、game.swf=1696FF25（未变属预期） |
| 启动自检 | `--check sa` / `--check sa_debug` 均通过 |

## 四、待实机确认项

1. 蓄力辉光（f2~f8 机身绿色辉光渐强）实机观感；
2. 枪口火焰同步回归 2.5 细节后的实机观感；
3. 待机画面随之变为 2.5 版（两版本设计几乎相同，理论上无感知差异）；
4. 开火音（#279，与原当前版同内容）触发正常。

## 五、文档与证据索引

| 文件 | 内容 |
|---|---|
| 粒子刃炮家族2.5-当前对比总结-20260911.md | 前置只读对比（蓄力缺失证实＋音频一致＋子弹差异） |
| 粒子刃炮家族蓄力段逐帧对照图-20260911.png | f2~f13 两版逐帧对照（蓄力有/无直观证据） |
| 粒子刃炮家族黑寡妇对比图-20260911.png | 蓄力四帧对照＋子弹对照 |
| 备份-被替换的旧资源-20260911/ | （见下）本任务旧 sub1130（1A5108E0）与旧配置（38EA3C12）备份 |
| 工作区（不入库） | `tmp-soya-family-test\widow-25gh-audit\`（widow_restore.py、merged XML、回读校验、逐帧渲染） |
