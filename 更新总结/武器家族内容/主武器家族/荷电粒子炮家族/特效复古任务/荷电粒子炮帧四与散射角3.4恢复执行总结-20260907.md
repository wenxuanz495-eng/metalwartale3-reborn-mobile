# 荷电粒子炮帧四与散射角 3.4 恢复执行总结

> 日期：2026-09-07
> 任务：①恢复荷电粒子炮（charged）LV1/LV3/LV4/LV5 本体开火辉光延续帧（帧四）；②恢复散射角 angleRange 至 3.4/2.5 原值。
> 范围红线：受击特效不改、主弹不改、材料/价格/commonLevel/伤害等数值不改。
> 状态：**已执行并部署，静态验证全过，待实机确认。**

## 一、帧四恢复（arms1100.swf）

**3.4 标准四帧流程**（以 LV1 #594 等实测）：

```text
帧1 待机本体 → 帧2 点亮版+细线框+炮口光斑+开火音效 → 帧3 空帧（辉光延续保持） → 帧4 回归待机本体
```

GitHub 现状缺帧 3 的保持帧（开火仅显示 1 帧即回归待机）。修复 = 在 #1141（LV1）、#1131（LV3）、#1126（LV4）、#1121（LV5）四个精灵的帧 2 结束后各插入 1 个 ShowFrame，frameCount 3→4。LV2 月读原本就是完整 4 帧，未动。

**验证**：
- 全树递归 diff + 多重集合对比（剔除 fileOffset 注解）：**恰好** 4 个 frameCount 3→4 + 4 个插入 ShowFrame，其余零变化；符号表 342 项不变；
- 帧渲染比对：四形态帧 2/帧 3 均显示开火辉光、帧 4 回归待机（对比图见本目录）。

## 二、angleRange 恢复（配置）

`decompiled\embedded-xml-assets\2_EmbedXml_xmlClass3_EmbedXml_xmlClass3.bin` charged 块内 5 处：

```text
12,12,12,12,12  →  10,10,10,15,15   （LV1-LV3 收紧至 10，LV4/LV5 放宽至 15）
```

编辑采用字节级定点替换（仅 charged 块 1636~1831 行，全文件其余 66 处 angleRange 未触碰）。

**落地验证**：`构建.bat` 的 build_swf 步骤将此 bin 作为 BinaryData #2 打入 game.swf；从 `build\game.swf` 导出全部 BinaryData 回读，charged 块 angleRange = `[10,10,10,15,15]` ✓（同窗口内下一家族重型粒子炮的 4 处 12 未受影响）。

## 三、部署与检查

- `arms1100.swf` 新哈希 `EC366FE645E7139424B100C3142E8986A1B972B380C5EA13CE2010AB21E110A1`，三副本（swf\、build\swf\、runtime\swf\）同步；
- 清单 `current-resource-manifest.sha256` arms1100 条目同步；
- `build\game.swf` 重建（build_swf.bat：7 个 BinaryData 补丁全部应用，[OK] SWF built）；
- 清单校验、`launch --check sa` 通过；实机冒烟启动正常。

注：构建时 build\server.exe 曾被运行中的游戏进程锁定（游戏会话运行中属正常现象）；因本次改动不涉及服务端，单独执行了 build_swf 步骤完成 game.swf 重建。**当前已开启的游戏会话加载的仍是旧资源，需重启游戏才能看到本修复。**

## 四、明确不做（范围红线）

- 受击特效：保持 `bullet/pink_energy`（与 3.4 一致，本来就不需改）；
- 主弹 bullet1：保持 GitHub 放大版（75×75）；
- 材料 mustItems、价格、commonLevel、伤害/能量/速度等：保持现状。

## 五、工作区与回滚

- 工作区：`D:\superalloy\tmp-soya-family-test\charged-frame4-restore\`
- 回滚：`input\arms1100.before.swf`（AB58AB17…）复制回三副本，清单 arms1100 条目改回 `AB58AB1796451A2A9380A25AEC89B288AA002BF1B76B78C7ABACD185D96AA948`；配置回滚 = 将 .bin 中 5 处 angleRange 改回 12 后重跑 build_swf。
- 仓库变更未提交：`swf/arms1100.swf`、`build/game.swf`（如被 git 跟踪）、清单、文档，提交待仓库所有者确认。
