# 电热化学炮家族 2.5 回迁执行总结

> 执行日期：2026-09-06
> 工作区：`D:\superalloy\metalwartale3-reborn.git`
> 回迁工作区：`D:\superalloy\tmp-soya-family-test\etcg-family-restore-20260906\`
> 依据：同目录《电热化学炮家族2.5与GitHub对比总结-20260906.md》及其确认的回迁计划

## 一、本次变更内容

**资源层回迁（唯一改动文件：`swf\arms1100.swf`，同步部署到 `build\swf\arms1100.swf`）：**

将 2.5 `arms_34.swf` 中电热化学炮家族十个导出的依赖闭包（并集 57 个 Character，新 Character ID **1652～1708**）整体导入，并将 SymbolClass 导出重指向：

```text
etcg_lv1: 1261 -> 1708      etcg_lv1_bullet: 1232 -> 1680
etcg_lv2: 1256 -> 1703      etcg_lv2_bullet: 1230 -> 1675
etcg_lv3: 1251 -> 1698      etcg_lv3_bullet: 1227 -> 1670
etcg_lv4: 1241 -> 1690      etcg_lv4_bullet: 1225 -> 1665
etcg_lv5: 1246 -> 1693      etcg_lv5_bullet: 1222 -> 1662
```

由此带回的 2.5 视觉（对应用户确认的计划）：

1. **开火特效停留时长**：本体时间轴 3 帧 → 4 帧，开火特效画面停留约延长一倍；
2. **枪口闪光描边色**：2.5 半透明黑 `RGBA(0,0,0,128)`（GitHub 旧版为白色不透明）；闪光本体为红半透明填充 `RGBA(255,0,0,51)`，属 2.5 原设计；
3. **开火特效层几何**：消除 GitHub 相对 2.5 约 (-12.7, -34.4) px 的绘制位置偏移；
4. **子弹尺寸**：lv1~lv3 恢复 2.5 的逐级小号子弹（52×29/56×33 → 62×34/66×38 → 72×39/76×42）；lv4/lv5 与原状一致；
5. **子弹矩阵细节**：lv3 恢复单位阵（去除 GitHub 的 0.9 缩放）；lv5 恢复 2.5 注册点；
6. **恒灭结构冗余消除**：开火帧不再重复放置与基体相同的形状（2.5 结构，开火帧深度1无放置）；
7. **蓝红闪烁机制**：两版本就一致（帧1 蓝 / 帧2 红），随导入自然保留。

**明确不改动项：**

- 受击特效：维持 GitHub 现状 `bullet/purple_motion`（配置 `hitImgLabel` 五处原样）；
- 配置文件：`2_EmbedXml_xmlClass3_EmbedXml_xmlClass3.bin` 零改动（imgLabel/bulletImgLabel 两版本同名，无需变更）；
- 射速、弹速、伤害、等级、研发材料、价格等全部数值；
- 旧 Character（1261 等）作为孤儿定义保留，未删除，不影响 wave/感应炮等其他家族；
- 开火音效 1240 未动（与 2.5 的 352 字节级相同）。

## 二、执行与验证记录

1. 合并脚本：`etcg-family-restore-20260906\merge_etcg.py`（十导出闭包并集 + 全量 ID 重映射，单 ElementTree 写盘，无重读陷阱）。
2. FFDec `xml2swf` 退出码 0 → `output\arms1100-etcg-2.5.swf`；`swf2xml` 回读退出码 0。
3. 回读八项静态验证全部通过：
   - 十个 etcg 导出齐全且唯一；
   - 十个新闭包无悬空引用；
   - lv1 本体 frameCount=4（2.5 结构）；
   - lv5 本体开火帧深度1无放置（冗余消除）；
   - 枪口闪光形状描边 = `RGBA(0,0,0,128)`（2.5 半透明黑）；
   - lv1 子弹位图 52×29 / 56×33（2.5 小号）；
   - lv3 子弹根放置无缩放（单位阵）；
   - 旧导出/孤儿完整保留。
4. 部署前备份：`report\backup\arms1100.swf.before-etcg-restore` 与 `arms1100.swf.build.before-etcg-restore`（旧哈希 97b0094e）。
5. 部署后哈希：`swf\arms1100.swf` 与 `build\swf\arms1100.swf` 一致 = `810bb2b0c7e5053fb3aba6d2702526a25696b98bf1cbf2c8f95dd5ab3fa65dbd`。
6. 资源清单已同步：`config\build\current-resource-manifest.sha256` 中 `swf\arms1100.swf` 更新为新哈希；`docs\CURRENT_RESOURCE_MANIFEST.md` 已追加 2026-09-06 批准记录。
7. `scripts\launch_game.bat --check sa` 自检通过（"Pure BAT game prerequisites are ready"）。

## 三、待实机确认（后续验证清单）

- 游戏内毁灭~恒灭五形态：开火闪光出现位置与红黑闪光观感、特效停留节奏（应为 2 帧停留）；
- lv1~lv3 子弹在战斗中的尺寸观感（应明显小于 lv4/lv5）及蓝红闪烁正常；
- 恒灭开火时炮体保持不变形（与 2.5 一致）；
- 感应炮（induction）与 wave（声波共振炮）等其他家族表现不受影响（其引用的旧 Character 未被改动）。

## 四、回滚方式

如需回滚：将 `report\backup\` 两个备份分别覆盖回 `swf\arms1100.swf` 与 `build\swf\arms1100.swf`，并把 `config\build\current-resource-manifest.sha256` 中该行恢复为 `97B0094E...`（旧哈希），文档记录删除本次批准条目即可。不涉及配置与代码。
