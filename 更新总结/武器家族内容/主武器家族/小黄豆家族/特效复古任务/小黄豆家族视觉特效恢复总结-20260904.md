# 小黄豆家族视觉特效恢复总结

日期：2026-09-04

## 处理目标

将 GitHub 仓库中的小黄豆家族视觉特效恢复为 2.5 版本的对应效果。子弹自旋参数按当前仓库保留，不作修改。

## 已完成修改

1. 保持此前已完成的视觉参数修改：小黄豆 LV1 后坐力为 `1.5`，子弹自旋 `bulletVra` 仍为 `0`，不改为 2.5 版本的 `-1000`。
2. 保持此前的第二发子弹视觉引用：LV2/LV3 使用 `arms/soya_lv1_bullet`，LV4 使用 `arms/soya_lv2_bullet`。
3. 将小黄豆六个等级的受击特效调用从：

   `sub/motion_hit_effect`

   回滚为仓库小黄豆覆盖前的调用：

   `bullet/yellow_motion`

4. `swf/sub1130.swf` 没有再次覆盖，继续使用仓库旧版资源；本次修正的是配置调用，而不是继续替换 `sub` 内的动画。

## 明确未修改的内容

本次只处理视觉表现相关内容，未修改武器伤害、等级、研发材料、研发要求、攻击速度、攻击间隔等实际游玩和战斗参数。

## 构建与检查结果

已运行仓库构建脚本 `构建.bat`，构建退出码为 0，成功生成 `build/game.swf`。

已完成以下静态检查：

- 构建后的 `build/game.swf` 中 BinaryData 字符 2 已回读确认六个小黄豆等级均为 `bullet/yellow_motion`；
- 当前 `build/swf/bullet.swf` 确实包含 `yellow_motion` 导出；
- `build/swf/sub1130.swf` 与 `runtime/swf/sub1130.swf` 哈希一致，均为仓库旧版资源；
- 四个 BAT 入口都调用同一个 `scripts/launch_game.bat`，正式启动器也查找同一个 `build` 目录；
- `scripts/launch_game.bat --check sa` 通过；
- 完整构建退出码为 0，生成的 `build/game.swf` SHA-256 为 `FA21AA2306F3FD1EE37109CB6BE17E102C0DD2EB0A9D3D3212CF580E3B3A1582`；
- 未生成错误的嵌套路径 `build/swf/swf/...`。

当前尚未完成游戏内实机验证，因此最终画面仍需在游戏中确认。

## 修改文件

- `decompiled/embedded-xml-assets/2_EmbedXml_xmlClass3_EmbedXml_xmlClass3.bin`

说明：此前文档中“已将 2.5 的 `motion_hit_effect_1～4` 导入 `swf/sub1130.swf`”的表述不适用于当前仓库状态；当前 `sub1130.swf` 未被本次修正覆盖，旧版 `bullet/yellow_motion` 才是这次实际恢复的调用目标。
