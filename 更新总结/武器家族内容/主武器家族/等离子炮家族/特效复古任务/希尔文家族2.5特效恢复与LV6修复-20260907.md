# 希尔文（plasma）家族 2.5 特效恢复与 LV6 修复

> 完成日期：2026-09-07
> 工作区：`D:\superalloy\metalwartale3-reborn.git`
> 前置审计：`D:\superalloy\tmp-soya-family-test\plasma-audit\希尔文（plasma）家族2.5-3.4-当今版对比审计-20260907.md`

## 一、本次完成内容

1. **LV1~LV5 子弹恢复 2.5 原版美术**（此前是共享核 585 + 1.0~1.5 缩放的离线重构版）。
2. **LV6 子弹**：无专属原版资源，按 LV5 子弹（2.5 美术）拉长放大制作临时替代，scaleX=1.2、scaleY=1.1（渲染约 147×44，LV5 为 122×40）。
3. **LV1~LV6 受击特效恢复 2.5**：配置 `hitImgLabel` 由 `bullet/blue_energy` 改回 `sub/chipped_hit_effect`。运行副本 `sub1130.swf` 本就含有该导出（id 1424），无需导入资源；`BulletBody.hitImgLabel` 的随机后缀逻辑只作用于含 `motion_hit_effect` 的标签，裸名直接通过。
4. **LV5 枪口火焰回归 2.5**：开火表现由 3.4/GH 的"整幅开火姿态图替换（1076）"改回 2.5 的"本体不动 + 独立焰斑叠加"。
5. **LV6 收火残留修复**：lv6 时间轴 f4 补 `RemoveObject2 depth=2`，枪口辉光（1071）不再残留于 f4 与循环帧。

## 二、资源层修改明细（arms1100.swf）

修改方式：FFDec XML 导出 → ElementTree 合并 → xml2swf 重建 → swf2xml 回读校验。脚本：`tmp-soya-family-test\plasma-audit\plasma_restore_merge.py`。

新导入（来自 2.5 `arms_34.swf`，全部为 DefineShape + DefineBitsLossless2 位图填充依赖）：

| 来源 (2.5 id) | 新 id | 用途 |
|---|---|---|
| shape 209 + bitmap 208 | 1739 + 1740 | LV1 子弹 |
| shape 206 + bitmap 205 | 1741 + 1742 | LV2 子弹 |
| shape 203 + bitmap 202 | 1743 + 1744 | LV3 子弹 |
| shape 200 + bitmap 199 | 1745 + 1746 | LV4 子弹 |
| shape 197 + bitmap 196 | 1747 + 1748 | LV5 子弹 |
| shape 214 + bitmap 213 | 1749 + 1750 | LV5 枪口焰斑（复用于 LV5 开火帧） |

Sprite 改写：

- `plasma_lv1~lv5_bullet`（1056~1060）：放置对象由共享核 585 改为对应新 shape，自然尺寸（去除缩放）。
- `plasma_lv6_bullet`（1055）：放置 1747（LV5 子弹），矩阵 scaleX=1.2、scaleY=1.1。
- `plasma_lv5`（1077）时间轴重建为 2.5 结构：f1 本体+挂点；f2 焰斑 1749+StartSound；f3 空帧；f4 仅移除焰斑（本体全程在 depth1 不离场）。
- `plasma_lv6`（1072）：f4 在本体还原后补 `RemoveObject2 depth=2`。

保持不变：lv1~lv4 本体时间轴、全部 smoke（仍用共享核 585）、其余所有导出（342 个 SymbolClass 完整）、本体/挂点坐标与音效。

配置修改（`decompiled\embedded-xml-assets\2_EmbedXml_xmlClass3_EmbedXml_xmlClass3.bin`）：仅 plasma 块（arms index="11"）内 6 处 `hitImgLabel` 替换，其他家族未动。

## 三、部署与同步

- 备份：`backups\plasma-restore-20260907\`（旧 arms1100 三份 + 旧配置 bin）。
- `swf\arms1100.swf` = `build\swf\arms1100.swf` = `runtime\swf\arms1100.swf`，SHA-256 均为 `0FBED3831C7220A35D8DA9F527A50DE442FD840E25065E998790FA1BCEB9C04B`。
- `config\build\current-resource-manifest.sha256` 中 arms1100 条目已更新为新哈希（有意变更，按批准清单流程）。
- `构建.bat` 全量构建通过（server、launcher、game.swf、175 个运行时资源校验、核心产物校验）。
- `build\game.swf` SHA-256：`011C251F5CF64F65DE92378690609B7510D5EFB919BD1F99020DFF0B41D8079A`。
- game.swf 内 BinaryData（字符 2）回读：plasma 六个等级 `hitImgLabel` 均为 `sub/chipped_hit_effect`，六个 `bulletImgLabel` 指向不变。
- `scripts\launch_game.bat --check sa` 自检通过。

## 四、验证证据

- 子弹像素比对：新 LV1~LV5 子弹与 2.5 原版逐帧**完全一致**（IDENTICAL）；LV6 为 LV5 放大 1.2/1.1。
- LV6 f4 渲染与 f1 待机**完全一致**（残留辉光已消除）。
- LV5 开火帧与 2.5 的差异仅剩挂点十字标记美术（8.88%，GH 保留 3.4 式白圈标记，属预期）。
- 证据目录：`tmp-soya-family-test\plasma-audit\render_new\`（新 SWF 渲染）、`postbuild\`（game.swf BinaryData 导出）。

## 五、实机反馈与追加修复

1. **拖尾重叠（实机反馈，已修复）**：首次部署后实机发现 2.5 子弹与当前版重构拖尾（共享核 585 渐隐球）同时显示，视觉重叠。处理：删除 plasma 块 6 处 `<smokeImgLabel>` 接线（全文件 190 个武器等级中 140 个本就无此标签，属"无拖尾"标准写法；`BodyGroupRefresh.as` 仅在 `smokeImgLabel != ""` 时生成拖尾）。`构建.bat` 重建后 game.swf 回读确认 plasma 块无 smokeImgLabel，hit/bullet 接线完好。新 game.swf SHA-256：`943BBD13DE63C6833BE67389BD949B7EF40FCC7FB4B52E69B0890AF0FE557676`。arms1100.swf 本次未改动（哈希仍为 0FBED383…）。
   - 备注：2.5 原版 plasma 其实带拖尾（每级独立焰形 smoke，lv5/lv6 沿用 lv4），如后续想还原 2.5 拖尾，可从 2.5 arms_34 导入 lv1~lv4 smoke 并按家族惯例接线。

## 六、第二轮：拖尾完全还原 2.5 + 碰撞参数调整 + 孤儿清理（2026-09-07 晚）

用户决策后执行：

1. **拖尾完全还原 2.5**：从 2.5 `arms_34.swf` 导入拖尾内核链（bitmap 189→1751、shape 190→1752、sprite 191→1753），GH 的 `plasma_lv1~lv4_smoke`（char 1066/1065/1064/1063）内容整体替换为 2.5 原版时间轴（共享内核 + 等级初始缩放 1.0/1.126/1.3/1.5，每帧 ×0.9444 缩小，5 帧生命周期；2.5 拖尾是"缩小型"，非共享核的颜色渐隐型）。**渲染比对：lv1~lv4 全部 20 帧与 2.5 原版像素级一致。**
2. **配置恢复拖尾接线**：lv1~lv4 各接本等级 smoke，lv5/lv6 按 2.5 家族惯例接 `arms/plasma_lv4_smoke`（2.5 无 lv5/lv6 专属拖尾）。
3. **碰撞参数（bulletWidth）**：确认该字段仅用于 HitIO 命中判定、不影响渲染。按恢复后子弹相对旧 GH 子弹的宽度比例放大：**9 / 14 / 19 / 25 / 31 / 34**（原 9/12/16/20/20/20；lv1 比例 1.03 维持 9）。计算式：`新宽 = 旧宽 × 新子弹宽 / 旧子弹宽`，如手感异常可单项回退。
4. **孤儿资源移除**：删除旧 LV5 开火整图（char 1076）与失去引用的 `plasma_lv5_smoke`（1062）、`plasma_lv6_smoke`（1061）及对应导出（导出总数 342→340）。**共享核 585/584 未删**——仍被通用拖尾 `conBullet_smoke`（char 586）引用。移除前完整副本与渲染存档见本目录 `备份-移除的孤儿资源-20260907\`。
5. 部署与验证：arms1100 三份副本（swf\、build\swf\、runtime\swf\）哈希一致 `D50D6CB28D6D6FA5AA811244AC1E9F88B84BC0815295799D3569E72455D98345`；清单哈希已更新；`构建.bat` 全量通过（期间清理了残留 server.exe 进程）；game.swf（SHA-256 `B9E98879206945F29D204508B3AA322A95503D9C9B6E1F49244F19F6007D0A6A`）回读确认拖尾接线 6 处、hit 接线与 bulletWidth 新值全部生效；`launch_game.bat --check sa` 通过。

## 七、仍需实机确认

- 拖尾还原 2.5 后的实际观感（缩小型焰形拖尾）。
- 新碰撞参数（9/14/19/25/31/34）的手感；如异常，可单项回退至 2.5 原值 9/12/16/20/20。
- LV5 枪口焰斑与 LV6 收火无残留的持续观察。

## 八、LV6 正式命名（2026-09-07）

经群内讨论定名：**LV6 冈格尼尔 → 提尔锋**（Tyrfing，标准音译）。选择理由：保留"出鞘必杀、不可阻挡"的诅咒魔剑意象，呼应 LV6 的破盾/反弹特性；音译采用最通行的标准写法。家族 Mythos 备考：希尔文（原型不可考）、雷沃汀（Lævateinn）、昆古尼尔（Gungnir）、米约尔尼尔（Mjölnir）、奥普（原型不可考）、提尔锋（Tyrfing）。

修改范围：仅 `decompiled\embedded-xml-assets\2_EmbedXml_xmlClass3_EmbedXml_xmlClass3.bin` plasma 块 LV6 `<name>` 一处 + `docs\baselines\玩家武器弹道速度分类与进度.md` 对应行（标注"原冈格尼尔"）。历史公告（runtime\公告.txt 中 2.4.x 时代描述）未回溯修改，下次发版公告建议注明"冈格尼尔已更名为提尔锋"。

构建验证：game.swf SHA-256 `E9322689B195215C6C5C1D3F935A6CBE4B3BD633CDA325A27BAE67DCADEB8C83`，BinaryData 回读 plasma 六形态名称为 希尔文/雷沃汀/昆古尼尔/米约尔尼尔/奥普/提尔锋，全文件无"冈格尼尔"残留。

## 九、遗留与后续可选

- LV6 子弹维持"LV5 拉长放大"临时方案，暂不新画。
- 备份文件夹 `备份-移除的孤儿资源-20260907\` 长期无用途后可整体删除。
- 下次发版公告注明 LV6 更名（冈格尼尔 → 提尔锋）。

