# 传说（激光脉冲炮）家族 2.5 开火特效、子弹与后坐力还原总结

> 执行日期：2026-09-05
> 任务来源：计划任务三项——开火特效回到 2.5、子弹回到 2.5、后坐力回到 2.5（受击特效按用户要求**不做更改**）。
> 工作区：`D:\superalloy\tmp-laser-family-restore`（隔离目录，标准 input/work/output/manifest/logs/report 结构）。

## 一、修改内容总览

| 项 | 修改前（GitHub/11.3） | 修改后（=2.5） |
|---|---|---|
| 开火闪光形状 | GH 重绘版辉光（1650 之前为 1377/1372/1367/1362） | 2.5 原版 4 张（新 ID 1650/1648/1646/1643） |
| 开火闪光时长 | 3 帧时间轴（闪光 1 帧） | 4 帧时间轴（闪光停留 2 帧，与 2.5 一致） |
| 子弹本体 | lv1 单独一张 + lv2/3/4 共用一张长拉丝 | 2.5 四张分级彗星弹（新 ID 1641/1639/1637/1635） |
| `recoilValue` | 0 | **2**（四级全部） |
| 受击特效 | `bullet/purple_energy` | **未改动**（按要求保留） |

未改动项：`bulletSpeed=30`、`attackGap=0.02`、`hitImgLabel`、双弹结构（`shootNum`/`tranArr`/`secondBulletImg` 含 LV3 主副弹互换）等所有数值与配置结构。

## 二、文件与 ID 变更明细

### 1. `swf/arms1100.swf`（新 SHA-256 前缀 BD477173...）

- 从 2.5 `arms_34.swf` 导入完整依赖闭包 **17 个标签**：8 个 DefineShape（4 开火 + 4 子弹）+ 9 个 DefineBitsLossless2 位图（辉光/弹体均为位图填充）。
- Character ID 全量重映射（目标原最大 ID 1634，分配 1635～1651）：

```json
{
 "463": "1635",  // 子弹 lv4
 "462": "1636",  "466": "1637",  // 子弹 lv3
 "465": "1638",  "469": "1639",  // 子弹 lv2
 "468": "1640",  "472": "1641",  // 子弹 lv1
 "471": "1642",  "478": "1643",  // 开火 lv4
 "476": "1644",  "477": "1645",  "484": "1646",  // 开火 lv3
 "483": "1647",  "489": "1648",  // 开火 lv2
 "488": "1649",  "494": "1650",  // 开火 lv1
 "493": "1651"
}
```

- 时间轴修改（sprite 1378/1373/1368/1363）：开火放置重指新形状 ID，StartSound 后的 ShowFrame 后补插一个 ShowFrame 停留帧，`frameCount` 3→4。开火音效（sound 127）不变（与 2.5 sound 479 字节级相同）。
- 子弹 sprite（1358/1356/1355/1354）内部放置重指各自新形状，恢复 2.5 的分级引用（原 lv2/3/4 共用 1353）。
- 矩阵修正 2 处：子弹 lv2/lv3 的放置矩阵按 2.5 原矩阵替换（lv1/lv4 原本就一致）。

### 2. `decompiled/embedded-xml-assets/2_EmbedXml_xmlClass3_EmbedXml_xmlClass3.bin`

- 仅在 `laserPulse` 区块内把 4 处 `<recoilValue>0</recoilValue>` 改为 `2`；区块外的 13 处 recoilValue=0（其他家族）未动。构建嵌入为 game.swf 的 BinaryData characterId 2。

### 3. 清单登记

- `config/build/current-resource-manifest.sha256`：arms1100 条目更新为新哈希 `BD477173D4A772E2BDB0F8D71EF395005AF1D944A348EF8027C63134C46F29E9`。
- `docs/CURRENT_RESOURCE_MANIFEST.md`：追加本次登记说明。
- `runtime/swf/arms1100.swf`：同步为新版（该目录此前遗留旧副本）。

## 三、验证链结果

| 步骤 | 结果 |
|---|---|
| 合并脚本写前自检（放置计数、frameCount、闭包完整性） | 通过（`logs/merge.log`） |
| xml2swf → swf2xml 回读 | 8 个 SymbolClass 名与 ID 不变；4 个武器 sprite 均为 4 帧且指向新开火形状；4 个子弹 sprite 指向各自新形状；17 个新定义全部存在 |
| 导入形状与 2.5 原版逐像素比对（8 组） | **全部 diffbbox=None，像素级一致** |
| 构建 `构建.bat`（build_all：server/launcher/swf/runtime） | 退出码 0；BinaryData 7 项全部替换成功 |
| 成品 game.swf 内嵌配置回读 | laserPulse 四级 `recoilValue=2`；`attackGap=0.02`、`hitImgLabel=bullet/purple_energy` 未被牵连 |
| 资源三副本一致性（swf/ build/swf/ runtime/swf/） | 哈希一致 |
| `scripts/launch_game.bat --check sa` | 通过 |

过程小插曲：首次构建时 `build\server.exe` 被残留进程（PID 41884）锁定报"拒绝访问"，结束该进程后重跑成功；首次清单更新脚本未生效（改为字节级替换后成功）。

## 四、工作区产物索引

```text
D:\superalloy\tmp-laser-family-restore\
  input\arms1100.swf.before        修改前 arms1100（SHA-256 A8976A2D...，见 manifest\backup-hashes.txt）
  input\xmlClass3.bin.before       修改前配置（SHA-256 F08629F6...）
  work\laser-merge.py              合并脚本（单 ElementTree 直接写盘，遵守鞭炮教训）
  work\merged.xml                  合并后 XML
  work\readback.xml                回读 XML
  output\arms1100.merged.swf       重建 SWF（与已部署版本同源）
  manifest\idmap.json              Character ID 映射表
  logs\merge.log                   合并与自检日志
  work\bdcheck\                    成品 game.swf 的 BinaryData 导出（验证用）
```

## 五、待实机确认

- 四个形态开火闪光时长/亮度在实机画面与 2.5 的一致性；
- 分级子弹的实际飞行长度观感（新弹体较短，视觉上弹道"头"更明显）；
- 开火后坐抖动恢复后的手感（`recoilValue=2`）；
- 回归项：敌我其他武器（尤其共用工字形路径 `arms1100.swf` 内其他家族）无异常。

## 六、回滚方式

```text
cp D:\superalloy\tmp-laser-family-restore\input\arms1100.swf.before  →  swf\arms1100.swf（build\swf、runtime\swf 同步）
cp D:\superalloy\tmp-laser-family-restore\input\xmlClass3.bin.before →  decompiled\embedded-xml-assets\2_...bin
清单哈希回写 A8976A2D... 后重跑 构建.bat
```

不要用 `git reset --hard`（工作区尚有其他未归属的在途修改）。
