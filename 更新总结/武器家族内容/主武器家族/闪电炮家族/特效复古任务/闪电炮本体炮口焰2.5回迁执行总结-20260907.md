# 闪电炮本体炮口焰 2.5 回迁执行总结

> 日期：2026-09-07
> 任务：将 2.5 版闪电炮（lightning）五形态的本体发射表现（炮口闪光/火焰、4 帧时间轴）回迁到 GitHub 版 `arms1100.swf`。
> 范围：**只动本体资源**。子弹、受击特效、配置数值（伤害/能量/后坐力/散射角等）全部保持现状。
> 状态：**已执行，静态验证全部通过；战斗内开火实测建议由玩家用测试存档做最终确认。**

## 一、执行方式

沿用《武器特效导入标准流程》与 `cannon-25-body-merge.py` 先例（依赖闭包 + Character ID 全量重映射 + SymbolClass 改指），隔离工作区：

```text
D:\superalloy\tmp-soya-family-test\lightning-25-body-restore\
  input\arms_34.swf            2.5 源 (SHA-256 7A7B7599…)
  input\arms1100.before.swf    迁移前目标备份 (SHA-256 E42BEDE0…)
  work\source.xml / target.xml swf2xml 全量导出
  work\lightning-25-body-merge.py   合并脚本（本次任务的可复用工具）
  output\merged.xml → output\arms1100.swf   xml2swf 产物
  output\verify.xml            回读校验
  output\new_sprites\          产物精灵逐帧导出
  manifest\input-sha256.txt / output-sha256.txt
```

## 二、合并脚本的三个关键适配

1. **闭包**：从 SymbolClass 定位 `lightning_lv1~5`（源 ID 322/317/312/307/302），递归收集本体子精灵、炮口焰形状、位图等共 **30 个元素**，全部重映射到目标未占用 ID 段 **1709~1738**。
2. **开火音效不重复导入**：源本体帧 2 的 `StartSound(#301)` 是 2.5 开火音，经解码 PCM 逐样本比对与目标现有 `#1196` 完全相同（0.679s/44100Hz/立体声）。脚本把 #301 排除出闭包，克隆体内的 `soundId=301` 统一改写为 **1196**——只迁画面，音频沿用现有资源（`DefineSoundTag` 数量迁移前后均为 31，无新增）。
3. **SymbolClass 改指**：`lightning_lv1~5` 五个导出名改指新 ID（1738/1733/1728/1723/1718）；旧本体精灵 #1197~1217 原样保留（3 帧旧资源成为无引用孤儿，不影响运行，回滚时只需改回符号表）。

## 三、验证结果（全部通过）

| 检查项 | 结果 |
|---|---|
| 符号表差异 | 342 个导出名集合不变，恰好 5 条 ID 变更，其余 337 条一字不动 |
| 新本体帧数 | 五形态均 **4 帧**（旧版 3 帧） |
| StartSound | 五形态均指向现有 `#1196`，位置同源（帧 2 炮口焰出现帧） |
| 逐帧像素比对 | 新本体 × 2.5 源本体：五形态 **全部帧 HASH=SAME，changed_px=0** |
| 子弹不受影响 | `lightning_lv1~5_bullet` 迁移前后像素一致 |
| 受击特效不受影响 | 配置未动（`bullet/purple_boom`），bullet.swf 未修改 |
| 标签数量 | Sprite +7（5 本体+2 子精灵）、Shape +10，无悬空引用（脚本强校验通过） |

## 四、部署与仓库同步

新 `arms1100.swf`（SHA-256 `AB58AB1796451A2A9380A25AEC89B288AA002BF1B76B78C7ABACD185D96AA948`）已同步三处：

```text
swf\arms1100.swf          源（SSOT）
build\swf\arms1100.swf    运行时读取
runtime\swf\arms1100.swf  构建校验副本
```

同步更新 `config\build\current-resource-manifest.sha256` 中 arms1100 条目为新哈希（清单校验机制：`prepare_build_runtime.bat` 开头会调 `verify_current_resource_manifest.ps1` 核对仓库文件哈希）。

**全链路检查记录**：

```text
verify_current_resource_manifest.ps1   → exit 0（2 条资源全部通过）
prepare_build_runtime.bat              → [OK] Runtime resources prepared: 175（首次运行时在 BGM 同步 copy 遇瞬时文件占用，手动复核各 copy 步骤均成功，重跑通过）
scripts\launch_game.bat --check sa     → [OK] Pure BAT game prerequisites are ready
```

注：`prepare_build_runtime.bat` 第 112 行有一个硬编码 arms1100 旧哈希常量（5DD480B1…），该常量位于 `goto copy_current_arms1100` 之后，属于**不可达死代码**（arms1100 走免检复制通道），未影响本次部署，亦未修改。

## 五、实机冒烟测试

启动 `启动游戏-flashplayer_sa.bat`（端口自选 + server.exe + 权威 Flash Player 34）：

1. 加载界面 → 标题界面（超合金合金战记 III，海豹版 1.2）正常；
2. 读取存档 → 测试（LV150）进入主城正常；
3. 武器库 → 主武器「雷丘」详情正常，家族五形态（托尔/因陀罗/宙斯/皮卡丘/雷丘）列表完整，本体渲染无缺资源；
4. 已用 `清理后台残留.bat` 清理本次启动的 Flash 与 server 进程。

战斗内开火的炮口焰实机画面（进副本开火）留待玩家用测试存档做最终确认——本体验证链已确认新时间轴与 2.5 原版逐像素一致，风险点仅在运行时表现。

## 六、证据图（本目录）

- 闪电炮本体回迁前后对比图-20260907.png —— 雷丘回迁前（3 帧弱辉光）vs 回迁后（4 帧强辉光）逐帧对照
- 闪电炮子弹lv1/lv5、本体lv1帧3帧4、开火帧与炮口焰、受击特效对比图 —— 迁移前对比审计证据（见对比总结）

## 七、回滚方案

1. 将 `input\arms1100.before.swf`（= 迁移前 `swf\arms1100.swf`，哈希 E42BEDE0…）复制回 `swf\`、`build\swf\`、`runtime\swf\` 三处；
2. `config\build\current-resource-manifest.sha256` 中 arms1100 改回 `E42BEDE079B8105C442F29F6FF7368D3C502D07690C1980A36D3702418B2834C`；
3. 无需改配置或代码（本次未动 game.swf / decompiled）。

## 八、备注

- 本次 git 工作区变更（未提交，由仓库所有者决定提交时机）：`swf/arms1100.swf`、`build/swf/arms1100.swf`、`runtime/swf/arms1100.swf`、`config/build/current-resource-manifest.sha256`，新增 `功能总结/武器家族内容/..\..\..\主武器家族\闪电炮家族`。
- 部署时检测到有一个**测试前已在运行**的旧 FlashPlayer 实例（PID 24184，非本次启动）未被清理；若其后仍被使用，建议重启后再做测试，避免读到旧资源。
