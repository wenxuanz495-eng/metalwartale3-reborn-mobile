# 斯巴达子弹缩放位移执行总结（消除枪口光斑遮挡）

> 执行日期：2026-09-11（**v3 当前生效**；v1/v2 已被同日 v3 取代）
> 前置：`斯巴达3.4与当前版对比总结-20260911.md` 第七节（遮挡定位，用户实机 GIF 印证）
> 用户决策链：新弹保留设计 → v1（×0.9＋右移165px，光斑完全让出）→ 用户实指"弹尾离枪口太远、弹体仍大" → v2（×0.75＋右移114px）→ 用户实机复测"亮斑距枪口仍差 ~41px，再缩小" → **v3（×0.65＋右移59px，亮斑起点正对枪口）**

## 〇、v3 当前参数（2026-09-11 生效）

- **矩阵**：`scale 0.65 / translateX 1180`（1180 twips = 59px）
- **弹体**：225×50 → **146×33**
- **定位依据（位图像素实测）**：bitmap534 前 54 列全透明，第 59 列（局部 x=−108）起才 ≥3px 不透明、第 77 列（−90）起实心——即"可见亮斑起点"在局部 −108，而非弹体矩形左缘 −167。v2 的空隙正是这段透明边距＋缩放所致（可见起点落在世界 ≈141）
- **v3 定位结果**：可见亮斑起点 = 111.2 − 108×0.65 + 59 = **100.0，正对枪口（x≈100）**；实心部分 107 起（射点附近）；弹头 207.9
- **遮挡评估**：光斑亮核（中心 x≈81.45）全程可见；弹体可见部分与光斑区右缘（119.4）相邻段为低不透明度渐隐边，1 帧后（+30px）完全飞离
- arms1100.swf SHA-256 = `D1184767CFBDDD1B67DF5E2FA9C28A493078CE429B01116619D9E3C41251709E`，三处同步＋manifest 已更新，`--check sa` 通过
- 相对 v2 的 XML diff 仅 1 行（矩阵行）；对最初版（9B02AAF0）除该矩阵行外均为 ABC `fileOffset` 连锁伪差异，无内容变化
- 勘误记录：v2 阶段文档曾把弹体左缘 −167 当"弹尾起点"，且一度误判弹体带"拖尾细线"——实机复核与位图像素实测确认弹体**无细线**，左侧仅为亮斑渐隐透明边距；本节参数以像素实测为准

## 一、目标

消除新版子弹（arc_lv1_bullet，225×50，弹尾锚点后延 167px）对开火枪口光斑区（本体坐标 x 43.4~119.4，光斑可见期 f2~f4 约 2~3 帧，attackGap 0.1s≈3 帧）的持续遮挡，使 3.4 同款枪口焰在持续开火时全程可见；同时按用户要求将新弹略微缩小。**不改弹体美术、不改碰撞（bulletWidth=10）、不改任何数值。**

## 二、实际修改（全仓库仅 1 行资源差异）

- **文件**：`swf\arms1100.swf` → sprite 536（`arc_lv1_bullet`）→ PlaceObject2Tag（characterId=535，depth=1）的 MATRIX：
  - 修改前：`hasScale="false" translateX="0" translateY="0"`
  - 修改后：`hasScale="true" scaleX="0.9" scaleY="0.9" translateX="3300" translateY="0"`（3300 twips = 165px）
- **参数推导**：弹尾局部 x=−167，×0.9 后 −150.3；世界弹尾起点 = 射点 111.2 − 150.3 + 165 = **125.9 > 光斑右缘 119.4**（余量 6.45px）；弹体变为 202×45。
- **同步部署**：`swf\`、`build\swf\`、`runtime\swf\` 三处已全部替换，哈希一致。
- **manifest**：`config\build\current-resource-manifest.sha256` 中 `swf\arms1100.swf` 行已更新为新哈希。
- **备份**：旧版副本与回滚说明见本目录 `备份-被替换的旧资源-20260911\`。
- **工作区**：`tmp-soya-family-test\sparta-bullet-shift-20260911\`（原始 XML、修改后 XML、output SWF、回读 XML、渲染与合成图素材）。

## 三、构建与验证

- 修改方式：FFDec swf2xml → 定点替换 → xml2swf 重建 → swf2xml 回读。
- 回读确认：sprite 536 矩阵 = `scale 0.8999939 / translateX 3300`（SWF 定点精度下的 0.9）。
- **全文件 XML diff 仅 2 行**（该矩阵行 −/+），其余 7.4 万余行零变化；`arc_lv1`(541)/`arc_lv1_bullet`(536) SymbolClass 与 ABC 类桩完好。
- 新 arms1100.swf SHA-256 = `6CC13184F22CCFA0693A83D7CB0044DB98121769E2FBB98E35D10D57D6247214`；三处副本 + manifest 哈希一致。
- `scripts\launch_game.bat --check sa` 通过（`[OK] Pure BAT game prerequisites are ready`）。
- 几何前后示意图：`斯巴达子弹缩放位移前后对比图-20260911.png`（本目录，按实测矩阵与位图精确合成）。

## 四、待实机确认项

- 实机持续开火：枪口光斑应全程可见（3.4 同款表现），弹体仍为新版设计、略小并从炮口前方少许位置窜出。
- 若观感上希望弹体更大/更小或再微调前后位置，仅需改本矩阵两个数值，成本极低。
- 遗留共性项（非本任务）：开火音挂 f2 两版实机均不触发（挂载帧位铁律）；`recoilValue`(当前=10) 与 `penetrationB=1` 是否保留待用户决策。

## 证据哈希速查

| 项 | 值 |
|---|---|
| 最初 arms1100.swf（修改前，备份留存） | `9B02AAF01985F58D6E82B6A0740F94214938304FA96426B778838F3688B48640` |
| v1 中间态（×0.9＋165px） | `6CC13184F22CCFA0693A83D7CB0044DB98121769E2FBB98E35D10D57D6247214` |
| v2 中间态（×0.75＋114px） | `702A052B0715D89A0C4CE109836A66E8D7EA7CC536C56469A1A69514A7B8DAC6` |
| **v3 当前（×0.65＋59px，亮斑正对枪口）** | `D1184767CFBDDD1B67DF5E2FA9C28A493078CE429B01116619D9E3C41251709E` |
| 修改差异 | 每轮各 1 行 XML（sprite536 PlaceObject MATRIX）；对最初版内容差异仍仅此 1 行 |
