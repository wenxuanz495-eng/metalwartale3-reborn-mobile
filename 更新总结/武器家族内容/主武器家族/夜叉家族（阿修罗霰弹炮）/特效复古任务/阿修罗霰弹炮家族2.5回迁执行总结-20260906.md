# 阿修罗（霰弹炮）家族 2.5 回迁执行总结

执行日期：2026-09-06
前置文档：《阿修罗霰弹炮家族2.5与GitHub对比总结-20260906.md》
任务内容（按用户要求）：子弹视觉回到 2.5、射速回到 2.5、后坐力回到 2.5；**受击特效不变**（保持 `bullet/red_motion`）。
隔离工作区：`D:\superalloy\tmp-shotgun-family-restore\`（input/work/output/manifest/logs/report）

## 一、修改内容

### 1. 子弹视觉回迁（SWF 资源）

文件：`swf\arms1100.swf`（源）、`build\swf\arms1100.swf`（运行）、`runtime\swf\arms1100.swf`（dev 暂存）

采用**原位替换**策略（引用唯一性已验证：sprite 1329 → shape 1328 → bitmap 1327 全链各仅 1 处引用），未引入新 Character ID：

| 项 | 修改前（11.3 系） | 修改后（=2.5） |
|---|---|---|
| 位图 1327 | 79×22，无弹头光斑长束 | **41×22，圆弹头+短拖尾**（2.5 位图 437 原数据，zlib SHA `804603c1...`） |
| 形状 1328 | 边界 X[-780,800]、边缘 ±1580 | **边界 X[-380,440]、边缘 ±820**（2.5 形状 438 原几何整块替换，shapeId 保留 1328、bitmapId 指向 1327） |
| sprite 1329 | 1 帧放置 1328，无缩放 | 未改动（回读与旧版逐字节一致） |

### 2. 射速回迁（配置）

文件：`decompiled\embedded-xml-assets\2_EmbedXml_xmlClass3_EmbedXml_xmlClass3.bin`

- `<arms index="4" id="shotgun">` 区块内 4 个形态 `<attackGap>`：`0.2` → **`0.15`**（=2.5 原值）
- 实际效果：齐射周期由 10 帧恢复为 8 帧（FPS=30），约 3 次/秒 → 3.75 次/秒

### 3. 后坐力回迁（配置）

- 同区块 4 个形态 `<recoilValue>`：`5` → **`2`**（=2.5 原值）

### 4. 明确未改动项

- `hitImgLabel`：四形态保持 `bullet/red_motion`（按用户要求不变）
- `bulletNum`/`angleRange`/`bulletTranslation`/`bulletSpeed`/`hurt` 等战斗与散射参数（与 2.5 本就一致）
- 武器本体、开火闪光、音效、挂点（三版本就同源，无可回迁项）
- 面板射速公式（`getShootSpeed` 的 0.067 开销常数为 11.3 全局改动，未单独处理；射速回迁后面板显示会随 attackGap 联动变化）

## 二、操作与验证记录

1. 隔离工作区备份源文件并记录 SHA-256（arms1100 基线 `bd477173...` 与既有基线记录一致）。
2. FFDec `-swf2xml` 导出目标/来源 XML → 整块替换（位图自闭合标签 + 形状对标签）→ 写盘前自检（关键导出、帧数、ID 计数）。
   - **踩坑**：块提取函数对 FFDec 自闭合标签（`<item .../>`，DefineBits 系列无子标签）处理错误，曾把位图标签与后续 shape 块一并吞入（len 7912 = 6044+1868），替换后导致 shapeId 1328 丢失；已修复为"先判断自闭合"并重跑。此前哈希正常但结构被吞，靠步骤断言拦截。
3. `-xml2swf` 重建（退出码 0）→ `-swf2xml` 回读校验：位图 41×22 + 2.5 像素 SHA 一致；形状 2.5 几何一致；五个 shotgun Sprite 帧数（4/4/4/4/1）不变；本体/闪光/音效/挂点引用不变；`SymbolClass` 前后 diff 完全一致（342 个导出）。
4. 新 SWF 部署三处（swf / build\swf / runtime\swf），三处哈希一致：`282B9540759545CA0E2A6F7D10D6F075D95BCB3B332858E77D532D618FE04A1A`。
5. 配置修改限定在 shotgun 区块内（脚本断言修改前值为 0.2/5 各 4 处，其余家族的 `attackGap=0.2` 10 处未受影响）；读档回验 4×0.15、4×2、hitImgLabel 4×red_motion。
6. `config\build\current-resource-manifest.sha256` 第 2 行更新为新哈希。
   - **踩坑**：heredoc 传参时 `\\` 被折叠成 `\a` 转义导致字节串匹配失败，改用 `chr(92)` 拼接后成功。
7. 清理后台残留（server.exe ×1、FlashPlayer.exe ×2）后运行 `构建.bat`：退出码 0，175 个运行时资源校验通过，核心产物验证通过。
8. 产物定点校验：解包 `build\game.swf` 中的 shotgun 配置区块，确认 `attackGap` 4×0.15、`recoilValue` 4×2、`hitImgLabel` 4×red_motion、imgLabel 四形态齐全。
9. `scripts\launch_game.bat --check sa` 通过。
10. 修改后子弹位图导出 PNG 与 2.5 原版并排比对：41×22 像素一致。见《霰弹炮子弹回迁后对比图-20260906.png》（上：2.5 原版；下：回迁后）。

## 三、涉及文件清单（均在 Git 工作区，未提交）

- `swf\arms1100.swf`（子弹位图+形状，新哈希 `282B9540...`）
- `build\swf\arms1100.swf`、`runtime\swf\arms1100.swf`（运行/dev 副本，已同步）
- `decompiled\embedded-xml-assets\2_EmbedXml_xmlClass3_EmbedXml_xmlClass3.bin`（射速+后坐力，8 行值）
- `config\build\current-resource-manifest.sha256`（arms1100 哈希行）
- `build\game.swf` 等构建产物（已重新生成）

## 四、遗留与实机验证清单

- [ ] 实机确认子弹视觉为 2.5 圆头短弹（购买/研发夜叉 lv1 后开火观察即可，四形态共用同一子弹）。
- [ ] 实机感受射速与后坐力手感是否回到 2.5（lv1 射速约 3.75 齐射/秒）。
- [ ] 受击特效维持 red_motion，如后续想改回 2.5 公共特效另行决策。
- [ ] 本批修改与火神炮批次（名称+枪口点）均未 git 提交，建议实机确认后一起提交。
- 隔离工作区 `tmp-shotgun-family-restore` 保留（含全部前后文件、哈希清单与日志），确认无误后可清理。
