# 4.0 本地版阶段 2 验收（本地可玩 + 启动器）

> 完成日期：2026-09-11
> 结论：**验收通过**。4.0 原版已可在本地完整游玩，用户实测通关序章关卡，战斗、存档、商店、加载链全部正常。

---

## 一、目标

按《4.0本地化前置审计-阶段0-20260911.md》的方案与用户三项决策执行阶段 2：
- 存档模型：**A（SOL 单槽，原版结构零改动）**
- 商店/M币：**用原版测试模式**（nowLevel=2）
- 反作弊：**整体禁用**

## 二、实际修改（全部在隔离工作区完成，正式仓库未动）

工作区：`D:\superalloy\tmp-soya-family-test\4.0-localize\`

**唯一被重编译的脚本：`Game.as`**（其余 647 个脚本保持原版字节码）：

| 补丁 | 内容 |
|---|---|
| 反作弊禁用 | `Game.zuobiTest()` 开头插入 `return;`（FFDec 优化后为空函数体） |
| 测试模式 | `checkVersion()` 中 `gameDefine.nowLevel = 1` → `2`（强制测试模式：免费商店分支 + "测试版"水印，HTTP 服务下原 file:/// 自动检测不触发） |
| 附带修复 | 为 `Game.as` 补 `import body.define.OneArmsDefine;`（重编译必需，原字节码无 import 概念） |

无需补丁即自动生效（审计预判正确）：`isLocal()` 真实判定（无壳时 serviceHold 保持 null）→ 跳过登录框、SeverTimeAPI 本地时间、LocalSave 直写 SOL、`readlist` 在线路径不被触达。

**交付物**（`原版\4.0（原版参考）\`）：
- `game-min.swf`：补丁版游戏本体（1,028,835 字节；文件名刻意与测试期一致，SOL 存档直接延续）
- `tools\FlashPlayer.exe`：仓库权威播放器 34.0.0.330（10,735,016 字节，与发布装包规范一致）
- `tools\server.exe`：主仓库 Go 服务端构建副本（本场景仅静态服务；自动创建的 `saves\` 目录本版游戏不使用；miniaudio 缺失提示可忽略，BGM 自动回退 Flash 内部音频）
- `启动4.0本地版.bat`：一键启动（随机端口 → 健康检查 → 开游戏 → 退出自动清理；端口冲突自动 +1 重试；延时用 ping，无 PATH 依赖；CRLF）

## 三、构建与验证

### 3.1 关键过程记录

1. **全量重编译失败教训**：首版用 FFDec 导入全部 648 个脚本重建，实机白屏卡死在 `swf/main400.swf` 加载后（服务器日志可见请求中止）。判定为 FFDec 全量重编译引入的行为回归（同类问题见 `docs/postmortems/FFDEC_CONTROL_FLOW_REGRESSION.md`）。期间用自研循环脚本自动补了 13 处跨包 import（解析 FFDec SEVERE 报错→定位类包路径→插入 import→重试）。
2. **最小侵入策略**：改为仅导入 `Game.as` 单脚本，其余字节码原样保留 → 一次成功，所有分包请求恢复正常。
3. **CDN 探测注意**：`s5.4399.com` 对缺失文件返回 HTTP 200 + 271 字节 HTML 错误页，**不能只看状态码**，必须校验 SWF 魔数（CWS/FWS/ZWS）。批量下载 125 个文件均通过魔数校验。
4. **GundamFunnel.swf 404 属预期**：无关卡引用，浮游炮资源在 bullet.swf（`BodyGroup.addGundamFunnel` 默认 father="bullet"）。

### 3.2 验证证据

- 补丁回读：重建 SWF 再导出 `Game.as`，确认 `zuobiTest()` 空体、`nowLevel=2` ✓
- 服务端三连测：`/api/status`、`/game-min.swf`、`/swf/bullet.swf` 均 200 ✓
- 实机（用户实测 + 截图）：主界面完整渲染（舰桥/军械库/研发/技能等导航 + 战车 + 版本号"当前版本号：4.0"）✓；商城打开、购买弹窗正常（"需要：100 M币(不足)"，余额校验生效）✓；"测试版"水印确认测试模式激活 ✓；**用户完整通关序章关卡，战斗/加载/音乐/存档全部正常** ✓

### 3.3 待办与已知事项

- ~~实机验证~~（已完成，用户确认）
- M币显示 NaN（4.0 原版遗留显示问题，与 11.3 离线版早期相同；不影响购买逻辑，阶段 3 修改器可顺带处理）
- SOL 路径绑定 `127.0.0.1/game-min.swf`：换文件名=换存档位；清档=删 `%APPDATA%\Macromedia\Flash Player\#SharedObjects\BDPJSB38\127.0.0.1\game-min.swf\yagao.sol`
- 每次读档仍会执行空 zuobiTest（无害）；`saves\` 目录由 server.exe 自动创建，可忽略
- 测试版水印与 M/P 调试键为所选方案（原版测试模式）的固有特征，用户已知情接受

## 四、待实机确认项

- [x] 标题页 → 主界面（用户确认）
- [x] 进入序章关卡并通关（用户确认"完美通关"）
- [x] 存档/读档闭环（用户确认正常；SOL 已验证写盘机制）
- [x] 商城购买（用户确认正常）
- [ ] 长时间游玩稳定性（后续自然验证）
- [ ] BAT 双击全流程（组件级已验证；首次双击如有问题反馈启动器窗口文字即可）

## 五、下一步（阶段 3：基础修改器）

- 字段映射已备好（审计第四节）：GCoin/MCoin(+镜像 GCoin2/MCoin2)、levelsLock、nowDifficult、nowGameLevel、playerName
- 修改器需直接读写 SOL 文件（AMF+deflate，与 Go store 同构可复用 amf.go）
- M币 NaN 显示问题顺带修复
