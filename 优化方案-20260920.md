# 手游整体优化方案 · 20260920

> 定位：配合《移植README.md》的整体移植前优化——**目录整理提案 + 帧数优化方案**。
> 帧数问题所有论断都有代码锚点（20260920 实测手游仓）；设计帧率 = 30fps（基线 SWF 头实测），实机达不到即为优化目标。
> 铁律：**先度量、再动手**——每项优化前后必须真机 A/B 对比，不凭感觉改。

## 一、掉帧根因清单（按嫌疑度排序，全部实测）

| # | 根因 | 证据锚点 | 影响 |
|---|---|---|---|
| 1 | `renderMode=direct` | `mobile\air-android\application.xml` | **direct 只给 Stage3D 用，本游戏无任何 Stage3D 代码 → DisplayList 全部回退 CPU 软渲染**。整幅画面由手机 CPU 逐帧栅格化，是低帧数的头号结构性原因 |
| 2 | `requestedDisplayResolution=high` | 同上 | 按物理分辨率渲染（主流机 2400×1080 级），软渲染下每帧填充量是逻辑分辨率的 4~8 倍 |
| 3 | `stage.quality="high"` 多处 | `Game.as:553/1050`、`Levels.as:336` | 软渲染下 HIGH = 全屏矢量抗锯齿，纯开销；`:1028` 已有切 low 的先例说明作者知道这开关 |
| 4 | `cacheAsBitmap` 全仓 **0** 处 | 全 `decompiled` grep | 背景/地面/静态装饰层每帧重栅格化（角色动画已由 BmpMovieClip 位图帧化，静态层反而没有缓存） |
| 5 | 战斗期实时滤镜 | `effect\Shock.as:44/101/102`（3×GlowFilter）、`effect\BezierLightning.as:31`、`effect\GhostingEffectBMC.as:85`（Blur）、`body\image\MultipleImage.as:492`（30×30 大发光）、`UI\gaming\LifeBar2.as:59`（血条发光） | 软渲染下滤镜 = 每帧区域像素级卷积；Shock/BezierLightning 是武器特效（闪电类），**开火即触发**——战斗掉帧的直接推手；血条滤镜更是常驻 |
| 6 | 子弹无对象池 | `body\bullet\BulletLink.as:33` `new OneBulletBody()`；全仓无 pool 痕迹 | 战斗高频分配 → GC 频繁 → 周期性卡顿 |
| 7 | 分散循环 | 19 个文件各自 `ENTER_FRAME` | 帧内执行顺序不可控，暂停/恢复难管理（有效但该整编） |

## 二、性能优化分层（P3 度量先行 → P0 零风险配置 → P1 缓存 → P2 逻辑）

### P3 度量先行（动手前必做）

1. 仓库现成 `test\Stats.as`（FPS/内存面板）——接到真机版采数：**确认瓶颈类型**。
   渲染填充率瓶颈 = CPU 不满但帧率低；逻辑瓶颈 = CPU 满帧率低。两类瓶颈的药方完全不同，不量就改=盲改。
2. 建立基准场景：同一关卡、同一段战斗（建议弹幕最密的波次）录屏计时，作为每项优化的 A/B 对照。

### P0 零风险配置层（只改配置/一行代码，先吃最大收益）

3. `requestedDisplayResolution`: `high` → `standard`——填充量降 4~8 倍，软渲染下单项收益最大；
   画质代价（颗粒感）真机评估，多数横版像素风游戏可接受。
4. `stage.quality`：战斗态统一 `low`（三处改掉；UI 静态界面如需清晰可局部恢复）。
5. `renderMode`: `direct` → `gpu` **真机 A/B 后定**——gpu 模式让 GPU 合成位图内容，但对手游用到的
   滤镜/BlendMode 有回退与表现差异，且 P1 没做之前收益打折。**建议顺序：先 3+4（保守轨）量收益，
   不达标再试 gpu 轨；两条轨都留存，按机型适配矩阵择优。**

### P1 渲染缓存层（小代码量，收益大）

6. 背景/地面/静态装饰层 `cacheAsBitmap=true`（滚动层配合 `cacheAsBitmapMatrix` 固定 1:1）——层移动只平移不重栅格化；
7. `LifeBar2.as:59` 血条 GlowFilter → 去掉或预烘焙发光贴图（UI 常驻滤镜不可接受）；
8. `Shock` / `BezierLightning` / `GhostingEffectBMC` 的实时滤镜 → **预烘焙帧序列**（美术烘好发光帧，走现成的
   BmpMovieClip 位图帧体系播放）——特效观感保留，运行时零滤镜；
9. `MultipleImage.as:492` 的 30×30 GlowFilter 同法处理。

### P2 逻辑层（中期，随 L3 移植批一起做）

10. 子弹/飘字/震荡波对象池（`OneBulletBody`、`RiseText`、`Shock` 复用实例），消 GC 抖动；
11. 19 处 ENTER_FRAME 整编进 Game 主 tick（XTimer 体系），帧内顺序可控；
12. tick 内高频分配治理（循环里 new Point/Rectangle/Array 的热点）。

### 与移植计划的衔接

- P0 三项 + P3 是**手游仓自有优化**，与端游回移零冲突，可立即做；每项改完 `application.xml`/AS 都要
  登记 `config\build\swf-script-patches.txt`（AS 部分）→ 构建 → 回读 → 真机验证；
- P1 第 8 条与 L3 的"武器特效回移"强相关：**建议顺序为先烘焙烘焙、后回移新特效**，否则端游 3.0 新特效
  （更多 GlowFilter 类）回移会让帧数进一步恶化——移植特效前先确立"特效必须预烘焙"的美术交接规范；
- P2 对象池属于结构性改动，与 L3 的子弹类回移（`OneBulletBody`/`ArmsAttack` 在端游独有 29 类里）合并做，一次改对。

## 三、目录整理提案（手游仓，待批准后执行）

对照端游 20260915 根目录整理（1cce2c8）的同款裁定，手游仓根目录现存以下旧制项，建议归档至仓内
`archive\root-legacy-20260920\`（只移不删，端游同款先例）：

| 条目 | 理由 |
|---|---|
| `build.bat` | `构建.bat` 的孪生副本（端游同款已归档） |
| `修改器.bat` / `start-modifier.bat` | `启动修改器.bat` 的重复入口（端游同款已归档） |
| `open-saves.bat` | 打开存档目录的近似重复（端游同款已归档） |
| `一键生成或更新静音版.bat` | 静音版流程 20260919 已停用（端游同款已停） |
| `战车属性为零修复.bat` | 超早期一次性修复，已过时（端游同款已归档） |
| `start-game-flashplayer_sa.bat` / `start-game-flashplayer_sa_debug.bat` | 与 `启动游戏-*.bat` 双语重复组（端游同款已归档） |

**联动必改**（红线：玩家入口不破坏）：归档前核对 `scripts\`、装包/引导脚本、BAT_RUNTIME 文档对上述 BAT 的引用，
同步改指向；`打开公告.bat` 的更名（打开公告目录 + 公告双文件制对齐端游）随 L1 公告对齐一起做。
`docs\` 旧扁平结构（重组前旧文档）暂不动——等 L1 移植启动时随端游 docs 新结构统一重组，本方案不扩大战线。

执行后：手游仓根 = 端游同款单一入口制（启动/调试/修改器/存档维护/公告目录各一个），另加 `mobile\`、`移植README.md`、`优化方案-20260920.md`。

## 四、验收口径

- 帧数：基准战斗场景真机平均帧率，P0 后 ≥24fps、P1 后 ≥28fps（30 满帧为最终目标）——以 Stats 实测为准；
- 观感：特效/血条/画质在真机截图对比中不明显劣化（劣化项逐个回滚或烘焙替代）；
- 回归：现有全部功能可用（触控、AIR 存档、礼包、公告），构建自检双 0；
- 每项优化一份简短记录入 bug 维护/开发商维护，附前后帧率数据。
